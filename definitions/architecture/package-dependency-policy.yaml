# Machine-readable package dependency policy (ARCH-GO-002).
#
# Consumed by tools/policy/depedge. Encodes the layer order
# kernel <- engines <- domains <- capabilities <- workflow <- platform/humanwork/transport <- application,
# with transaction/ledger/data modelled as ports/adapters rather than ranked
# layers: business code depends on the port (the package itself, e.g.
# internal/transaction or internal/data), never on a concrete adapter
# subpackage underneath one.
#
# Roots the layout manifest declares outside this spine (intent,
# governance, trust and its adapters, connectivity and messaging,
# operations, the data-mesh evidence/documents/resource/contractarchive
# roots, and the collaboration/experience/forms/a11y/i18n surfaces) stay
# deliberately unranked: their mutual edges form a mesh no total order
# models, and ranking any of them would trade real violations for waiver
# noise. They are placed by repository-layout.yaml, not by rank.
#
# See planning/specs/platform-responsibility-boundaries.md and
# planning/specs/platform-plane-model.md for the normative dependency
# invariants this manifest turns into an evaluable rule set.

version: 1
module: github.com/monstercameron/human-capital-management-suite

disposition: >-
  Rules describe semantic ownership direction, not directory-depth
  heuristics: a rule names a layer/port relationship, and roots are only
  how that relationship is currently located on disk.

# Ranked layers. Rank order encodes the allowed dependency direction: a
# package may import a layer with a lower-or-equal rank; importing a higher
# rank ("upward") is forbidden unless an explicit exception applies.
layers:
  - name: kernel
    rank: 0
    roots:
      - internal/kernel
  - name: engines
    rank: 1
    roots:
      - internal/engines
      - internal/conformance
  - name: domains
    rank: 2
    roots:
      - internal/domains
      - internal/commercial
      - internal/customobject
  - name: capabilities
    rank: 3
    roots:
      - internal/capability
  - name: workflow
    rank: 4
    roots:
      - internal/workflow
  - name: platform
    rank: 5
    roots:
      - internal/platform
      - internal/effectgraph
      - internal/flow
      - internal/generated
      - internal/performance
      - internal/replan
      - internal/store
      - internal/configuration
  - name: humanwork
    rank: 5
    roots:
      - internal/humanwork
  - name: transport
    rank: 5
    roots:
      - internal/transport
  - name: application
    rank: 6
    roots:
      - internal/application

# business_layers identifies which ranked layers are "business packages" for
# the concrete-adapter rule: they may depend on a port, never on the
# concrete adapter underneath one.
business_layers:
  - domains
  - capabilities
  - workflow

# Ports/adapters are not ranked against the layers above. A layer may import
# the port package itself (e.g. internal/data, internal/ledger,
# internal/transaction); it may never import a path matched by
# concrete_adapter_markers below, which names the implementation
# subpackages underneath a port.
ports_and_adapters:
  - name: transaction
    roots:
      - internal/transaction
  - name: ledger
    roots:
      - internal/ledger
  - name: data
    roots:
      - internal/data

# Path globs (one "*" wildcard segment, matched against the module-relative
# import path) identifying a concrete adapter implementation rather than a
# port interface. Business layers (business_layers, above) must not import
# these paths directly.
concrete_adapter_markers:
  - internal/data/postgres
  - internal/data/*/postgres
  - internal/data/adapters
  - internal/data/*/adapters
  - internal/connectivity/*/adapters
  - internal/transaction/adapters

# Path globs identifying a domain's own persistence subpackage. Workflow
# must never import these directly; it reaches domain state only through
# domain capabilities.
domain_persistence_markers:
  - internal/domains/*/store
  - internal/domains/*/persistence
  - internal/domains/*/repository

# Forbidden-edge rules, named exactly so violations can cite one identifier.
# tools/policy/depedge implements each rule; this list documents intent and
# gives CI output a stable vocabulary.
rules:
  - name: kernel-must-not-import-upward
    description: >-
      internal/kernel may not import any higher-ranked layer or any
      port/adapter. Kernel stays dependency-minimal (ARCH-GO-004).
  - name: engine-must-not-import-domain-implementation
    description: >-
      internal/engines/* may not import internal/domains/*; domains adapt
      their facts into engine requests, not the reverse (ARCH-GO-008).
  - name: workflow-must-not-import-domain-persistence
    description: >-
      internal/workflow/* may not import a path matched by
      domain_persistence_markers; workflow coordinates typed capability
      results, never domain tables directly (ARCH-GO-007, platform-plane-model.md).
  - name: transport-must-not-import-store
    description: >-
      internal/transport/* may not import a data or ledger port/adapter
      directly; transport adapts protocol only (ARCH-GO-002).
  - name: business-must-not-import-concrete-adapter
    description: >-
      A business_layers package may not import a path matched by
      concrete_adapter_markers; it depends on the port package instead.
  - name: layer-must-not-import-upward
    description: >-
      Any package in a ranked layer may import a layer with a
      lower-or-equal rank, never a higher one. This is the general form
      of the direction the named rules pin for their exact cases; it runs
      after them so their citations stay stable where both match.
  - name: product-must-not-import-tools
    description: >-
      Packages under internal/ and cmd/ are part of release binaries and may
      not import developer packages under tools/; shared product code belongs
      under internal/ and tools may depend on that code in the other direction.

# Reviewed, narrow exceptions. Each entry names the exact importer/imported
# pair (module-relative import paths) it waives, plus owner/rationale/expiry
# per the ARCH-GO-002 GREEN clause. Widening scope always requires a new
# entry, never editing an existing one in place beyond its expiry review.
# Every entry below names a live-graph edge REV-101-02 verified and may not
# move itself: workflow step adapters consume the human-work vocabulary
# from packages this todo does not own, and the sandbox composer drives
# the application root by design. The leave -> workitem edge is fixed,
# not waived.
exceptions:
  - importer: internal/platform/sandbox
    imported: internal/application
    owner: platform-foundation
    rationale: >-
      The sandbox composer builds fenced Promotion tenants through the
      application composition root (ComposeExecutionAuthority); the edge
      is the designed composition direction for fenced tenants.
    expiry: "2026-12-31"
  - importer: internal/workflow/cancellation
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/execute
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/execute
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/execute/effects
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/inspect
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/migrate/artifacts
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/promotionexec
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/promotionexec
    imported: internal/humanwork/approverclass
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/promotionexec
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/prototype
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/simulate
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/simulate
    imported: internal/platform/timeauth
    owner: workflow-runtime
    rationale: >-
      The workflow simulator reads the platform time authority directly;
      narrow waiver pending a below-domains extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/steps/approval
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/steps/approval
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/steps/task
    imported: internal/humanwork
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/steps/task
    imported: internal/humanwork/workitem
    owner: workflow-runtime
    rationale: >-
      Workflow step adapters consume the human-work approval/work-item
      vocabulary directly; narrow waiver pending a below-domains
      extraction follow-up.
    expiry: "2026-12-31"
  - importer: internal/workflow/steps/task
    imported: internal/humanwork/formcontinuity
    owner: workflow-runtime
    rationale: >-
      The TASK intake boundary (REV-025-01) establishes alternate-channel
      continuity records as submission evidence; the record keeps the
      respondent's authority and grants the route no decision power.
      Narrow waiver pending a below-domains extraction follow-up.
    expiry: "2026-12-31"
