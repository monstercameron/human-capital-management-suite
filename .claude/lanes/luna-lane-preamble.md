STANDING RULES (hcm-next, C:\Users\mreca\Desktop\hcm-next; Go 1.26.3 windows/arm64, no -race, no Docker; embedded PostgreSQL tests via internal/data/pgtest — `pgtest.New(t)`, `TestMain(m) { pgtest.RunMain(m) }`):

- Read planning/todos.md for your todo ids: each block has Depends / INTENT CONTEXT / TEST / TEST MATRIX / RED / GREEN / REFACTOR / Refs. Implement to GREEN; the PRIMARY test must be named exactly as the TEST field says; matrix tests (Golden/Integration/Security/…) use the TestTodo*<ID>*<Kind> names where feasible, otherwise name what you built and say so.
- Every hand-written .go file you create gets a \_test.go in its package. gofmt -w only files you create/edit. Run `go vet ./<pkg>/` and `go test -count=1 ./<pkg>/` targeted (root `go test ./...` times out). Import prefix github.com/monstercameron/human-capital-management-suite.
- Work on local `main`; do not create a branch or worktree. You may edit code, tests, planning, definitions, migrations and go.mod/go.sum when your assigned todo requires them. Coordinate overlapping files and migration numbers with other agents. Only one agent stages or commits at a time: inspect status and the staged diff, stage explicit paths, verify the staged diff contains only your work, and commit through the full hook. Never sweep another agent's changes into your commit, bypass hooks, stash, hard-reset, amend, rebase or force-push. New tables need RLS `tenant_isolation`, a forbid_mutation trigger where append-only, and a storage-disposition row. Do not widen a policy allowlist to fix a placement failure.
- Verify with build/vet/test, then run `go test -count=1 ./tools/policy/libfirewall/ ./tools/planning/boundarytests/ ./tools/policy/importgraph/` and report any failure naming YOUR packages verbatim (there are pre-existing failures in internal/domains/budget, internal/i18n, internal/platform/cache, internal/resource/reservation, internal/transport/eastwest, tools/uxqual/wcag and ~40 undeclared internal roots from another session — those are not yours).
- Report: per todo — files created/edited, test names with PASS output pasted, the evidence sentence in this exact shape: `<TestNames…> in <pkg>; go test -count=1 ./<pkg>/ PASS on windows/arm64 (Go 1.26.3); commit <sha> on local main` — plus anything you could not finish (say "partial" and why).
