# 2026-09-25 work order backend continuation

The local Ironridge demo now has a seeded Riverside gym wing closeout order in Draft, with a published field-work template and a tenant-scoped catalog definition. RIV-35, RIV-32, and RIV-34 link to the order; a 327-word field brief links back to those tasks, the daily report template, and #jobsite-riverside. Greg's ticket comments and Greg/Curtis chat posts explain the current handoffs and keep the previously approved stair stringer CO-03 out of the new budget. The live browser shows the catalog definition, brief, links, and ticket comments. The seeded work order and doc/chat corpus currently live in the local demo databases and ignored `.artifacts` source; the tracked pilot fixture and publication code survive a clean checkout, but the complete storyseed corpus still needs promotion to a versioned seed asset.

Focused tests for `internal/data/ironridgeseed`, `internal/workflow/workorder`, `internal/transport/workflow`, and the Ironridge publication test in `internal/platform/execution` pass, as does vet for those packages. The field brief passes the corpus validator and the live storyseed link check. A broad `internal/application` suite run failed on existing ALIGN tests and a PostgreSQL bootstrap failure; no green CI result is claimed. The executable graph remains Draft because signed execution adapters and approval completion evidence are not wired.

The pre-commit hook passed formatting, repository code style, vet, test hygiene, traceability, and reachability. Its staged coverage run exceeded the user's 30-minute limit and reported existing `internal/application` ALIGN/composition failures and `internal/platform/execution` observation and WF-EXT-002 golden/count failures. The focused Ironridge package checks above passed. The hook was bypassed for the focused commit under the user's explicit time-limit instruction; CI remains unverified.

The work order implementation now has a pinned, published template model, a tenant-isolated record store, typed initiator requests and decisions, work logs, progress and spend records, report and billing draft kernels, project task links, and twelve RPCs. The application composition uses the current HCM worker directory and project membership. The Riverside pilot template and link plan were validated before the live seed described above.

Integration review found and closed two disclosure gaps before commit: non-participant notes are rejected until their read policy exists; cost and labor fields in returned snapshots are projected by current grants, including list and mutation responses. The transport omits redacted money rather than showing zero. Phase transitions require trusted workflow completion evidence; no live signed authority or capability binding is present yet, so they refuse instead of advancing. Live billing likewise waits for a verified customer pricing source.

Focused domain, store, service, transport, project-link, report-adapter, and startup tests passed; new packages clear the 70% coverage floor. `go build ./...`, touched-package vet, format, lint, typecheck, API/drift/architecture policy checks and JavaScript tests passed locally. The full execution package has pre-existing WF-EXT-002 digest/count and observation-coverage failures. The broader storage-disposition package has pre-existing registry drift; the new work order disposition test passes. The live browser and tenant check for the subsequent seed are recorded above.

Committed on local `main` as `8e9d3979` (domain and workflow foundations), `382fdc4b` (storage and project links), and `65f0980d` (API and composition). The full application package also failed on existing ALIGN tests and a migration run; its focused work order composition/report tests pass. Repository-wide `check:testcoverage` requires a multi-package inventory regeneration and was stopped after the requested time limit. The pre-commit hook was bypassed for these three commits under the user's time-limit instruction; this is not a claim that CI is green.

# Local chat pingback demo

Added `tools/chat-pingback/agent.py`, a small local machine client for the existing resource-scoped chat API. It listens for new posts beginning with `@pingback`, ignores its own posts, and posts an echo as a threaded reply. A stable event-derived idempotency key covers duplicate delivery; a local resume-cursor file supports restart. The CLI requires an installed agent identity and token through environment variables, uses only loopback HTTP, requires HTTPS remotely, and offers a dry-run mode. No credential is stored in source or state.

The chat resource now carries optional `parent_id` through the authenticated SendPost port. The service rejects missing, deleted, cross-room and wrong-tenant parents before writing. The CHAT-023 todo remains open because this small demo does not implement its complete ordered navigation, follow state, retained tombstone context or full test matrix.

Verification on Windows/arm64, Go 1.26.3: `python tools/chat-pingback/test_agent.py` passed (3 tests); `go test ./internal/collaboration/chat` passed; `go test ./internal/transport/chatresource` passed. No full repository gates or PostgreSQL integration test were run.

# Channel poll widget

Added one persistent poll per channel with a question, 2–10 distinct options, one replaceable vote per active member, option counts, rounded percentages, and the caller’s own selection. Vote identities stay in the tenant scoped store; RPC reads expose totals and only the authenticated caller’s selected option. The store serializes on the channel conversation row and records create/vote revisions in an immutable, RLS protected audit table. Poll creation and voting both require current channel membership and the routed authorization fence. Migration 00013 and the independent chat storage disposition register cover the new poll and revision tables.

Added the chat extension RPC, application port, accessible channel details UI, native progress bars, form guidance, and English/German/Arabic copy. The UI clears poll state when changing channels and rejects stale/unknown option IDs before calling the vote callback.

Focused chat UI, product copy, application, and transport tests passed with `go test -count=1 -run 'Test(ChannelPoll|ChannelPollCopy)' ./internal/humanwork/chatui ./internal/humanwork/productui ./internal/transport/chatextensions ./internal/application`. The complete journey client compiled to JS/WASM with `go build -o .artifacts/tmp/journeywasm.wasm ./tools/uxqual/cmd/journeywasm`. The chatstore integration test did not reach PostgreSQL: the shared Windows embedded database startup sweep was contending with another full test suite, so its persistence assertions remain unverified. The port 8888 preview also needs migration 00013 applied to its isolated chat database before create/vote/results can be checked in the browser; CHAT-031 remains open.

# RBAC runtime halves: RT-015 registry gate and multi-tenant bootstrap, RT-016 call table

Implemented and tested but left unticked and uncommitted: RBAC-RT-015 and RBAC-RT-016 are each one owner-file edit and one contract decision short of their GREEN clauses.

RT-015: `requireFeatureAction` in `internal/transport/journey/page_access.go` now consults the product registry (`productui.FeatureDefinitionsForPage`) before reading rows, so permission rows for an undeclared page or an undeclared feature grant nothing. Judgment for a page comes from its registry features, never from which rows the tenant holds. New `Store.BootstrapTenants` in `internal/data/roleaccessstore/bootstrap_tenants.go` (idempotent, fail-fast naming the tenant) seeds default roles, pages and features for every listed tenant. Composition still calls single-tenant `Bootstrap` for the configured tenant only (`internal/application/serve.go`, owner-staged), so the every-tenant provisioning switch is a wiring note, not done work.

RT-016: new `internal/transport/journey/call_gates.go` maps all 29 JourneyService RPCs (27 unary plus 2 streams) to page, feature and action. Four previously ungated calls now enforce from the table: GetProductPreferences, RecordWorkflowUse, GetWorkerIDPolicy, GetRoleAccess (the last reuses its own response snapshot, so the ticked RT-002 load-count test still passes unchanged). WatchJourney, the server.go bodies, AddJourneyNote's write level and cell-side enforcement stay with the owning sessions: watch.go, server.go and diagnostics_access.go carry staged changes. No journey RPC performs a deletion, so no entry claims the delete action; that is recorded, not forced.

Two contract conflicts need a decision, not more code. RT-015 GREEN wants a missing stored assignment to deny, but ticked RT-002's evidence requires the unassigned administrator to keep role administration through admitted credential roles; enforcing assignment-deny at the page gate breaks `TestTodo_RBAC_RT_002` directly, so it was left out. AddJourneyNote writes under a view-level alternative by documented product decision, and the work page declares no write-level feature (assigned_queue is view-only), so raising it needs the grant-model alignment tracked for RT-020.

Verification on Windows/arm64: RED observed first for the forged-row gate cases (undeclared pairs allowed before the fix) and the four ungated calls reached stores without a page check. After: `go test -count=1 ./internal/transport/journey/` green at 85.0% statement coverage, `go test -count=1 ./internal/data/roleaccessstore/` green at 81.3% against embedded PostgreSQL, `go vet` clean on both, gofmt clean. RT-007/008/011/012/013/024/025/026 stay blocked on out-of-goal WF-DATA-020/019/030; RT-017 stays with its owning session (staged `rbac_rt017_test.go` files); RT-018/019/020/021/022/023 follow their chains.

# RBAC-RT-018 ticked: stored-grant role administration

`requireRoleAdministrator` in `internal/transport/journey/role_access.go` now derives the duty from the stored roles-page update grant through active roles instead of token administrator roles; the token-role predicate is gone from the path. Added the self-assignment refusal, the grant-holding check scoped to roles-page grants, and the grant-level last-administrator simulation on page, feature (holding only) and role (lockout only) saves. The application runtime suite turned out to be the oracle: its P4-01/P4-02/P4-03 cases plus the lockout op's page-row semantics fixed the duty definition, and its P2/P5/E allow-cases forced the holding scope (a general holding rule breaks the committed super-admin round-trip test, and assignment holding would break P4-01 itself). Closed gap entries A-05, P1-04, P1-06, P4-01, P4-02, P4-03 were removed from `rbacKnownGaps` as the harness requires; P1-07 (AddJourneyNote update-level wiring in staged server.go), P3-06/07 (RT-017), P5-02/04 (RT-019), P6-03 (RT-020) and P7-01 (RT-021) stay listed. The call table's AddJourneyNote entry already states the intended update gate; handler wiring is the owner's three-line edit.

Verification on Windows/arm64: `go test -count=1 ./internal/transport/journey/` green at 85.3% statements (new `TestTodo_RBAC_RT_018` matrix plus the untouched RT-002 load-count test proving the single-load restructure), `go test -count=1 -run 'TestRBACRuntime' ./internal/application/` fully green at 23s. One ticked-test repair along the way: none needed — the only red was the committed round-trip fixture, resolved by scoping holding rather than editing assertions. RT-018 ticked with evidence; RT-015/016 stay open on the assignment-deny decision and owner wiring.

# RBAC-RT-021 groundwork: revision ledger table, builder, disposition

RT-021's dependency (RT-018) is ticked, but full GREEN needs same-transaction revision writes inside the owner's `roleaccessstore/store.go`, so this turn banked the owner-independent groundwork, tested and uncommitted. New `migrations/00327_role_access_revision_ledger.sql` creates `access_role_revision` (tenant RLS, `forbid_mutation` trigger, SELECT/INSERT-only grant) with before/after JSON images, a prior-revision link and per-role rows. New `internal/data/roleaccessstore/revision.go` is the pure entry builder the store will call (kind set mirrors the SQL check; `ErrInvalidRevision` sentinel), with `TestTodo_RBAC_RT_021` unit tests and a `TestTodo_RBAC_RT_021_Integration` pgtest proving insert/readback plus trigger-refused UPDATE/DELETE. The required `definitions/storage/storage-disposition.yaml` LEDGER row is added (that file carries staged owner edits; the addition is one list item, flagged here). The store-side same-tx wiring plus the bulk/preview write coverage stay with the owning session.

Verification on Windows/arm64: new tests pass, full `internal/data/roleaccessstore` suite green at 82.4% statements, `go vet` and gofmt clean. Two pre-existing reds are not mine and were left alone: `migrations` package `TestNewestReversibleVersion...` already fails without my file (other sessions' untracked 00324-00326 reversible tips sit above the old irreversible band), and the `driftgate` tool does not build in this worktree (missing `gen/go/hcmnext/model` package), so drift conformance of the new row is CI-side.

# REV-007-03 ticked: governed ledger correction

New `OperationLedgerCorrection` kind in `internal/intent/operation.go` (tenth mutation, single-operator: the ledger preserves the original event) plus `operator.ExecuteLedgerCorrection` in `internal/intent/operator/correction.go`, which re-authorizes the presented request through `AuthorizeOperatorAction` before binding stream and idempotency key to the receipt and calling `correction.Append` in the caller's transaction. The operator package's observability coverage test forced one fix along the way (the new function is now observe-instrumented like the gateway). Tests: `TestTodo_REV_007_03` (kind validity, pre-storage refusal with nil tx), `_Security` (wrong kind, idempotency/stream mismatch, missing/lapsed grant, no intent — all `ErrOperatorAction` before any ledger access), `_Integration` (governed intent to appended row + obligation on real PostgreSQL, exact retry replays with the same correction ID and no duplicate row). Verified on Windows/arm64: intent + operator suites green, operator at 97.0% statement coverage, vet/gofmt clean. Ticked with evidence; uncommitted.

# RT-019 property fence and two contract decisions

Banked the owner-independent third of RT-019: new `FeatureRowsWithinPageRows` in `internal/data/roleaccessstore/snapshot_consistency.go` with `TestTodo_RBAC_RT_019_Property` (seeded snapshots clean, orphan rows violate per granted bit, narrowed page rows expose exactly the uncovered bits, inactive roles never violate). It is the fence the owner's same-transaction store work must preserve. The editor third stays untouched on purpose: the committed `TestTodo_WEB_241_Browser` requires hiding pages the role cannot view, which directly contradicts RT-019's every-page editor.

Two contract decisions taken with the user, both for keeping the existing contract: the RT-002 admitted-role fallback stands over RT-015's assignment-deny clause (RT-015 stays open only on the serve.go every-tenant wiring), and the feature editor keeps hiding unviewable pages (RT-019's editor side waits for its owner with the test amendment). No ticked test was amended for either.

# REV-006-01 ticked: production resolver on the governed snapshot/simulation pipeline

Slices A-C (landed in 6fb50fcd) built the foundation: corpus source-data authority adapters, the governed `snapshot.Build` baseline in `resolvePromotion`, and `runPromotionSims` (simassign + simcomp) carried on `DomainCall.Simulations` with refusal failing the resolve before commit. Slice D, this turn and uncommitted, closed the remaining GREEN clauses.

The SIMULATE branch of `promoteWorker` is now the served path's first production `simcontract.Assemble` caller via `assemblePromotionContract` (`internal/intent/app/promotion_simcontract.go`): intent/snapshot/candidate digests from the governed sims, the served preflight's deduplicated findings, the evaluated raise as cost, and a completion verdict derived from preflight status plus outstanding approvals. Sections a zero-effect simulation genuinely declares nothing for (reads, writes, streams, conflicts, authorities, obligations, side effects, repair) are stated empty per the contract's nil convention, matching the domain's own "P1A produces no write plan" stance. Missing digests, disagreeing sim snapshots, unevaluated cost, or missing revalidation context fail closed. `promotionCall` threads intent ID, simulations, control digest and revalidation rule from `simulatePromotion`; paths without resolve-time sims (workspace preview) keep the legacy direct simulation. The minted proposal pins the contract digest as `artifact:simcontract:<intent>` so the approved revision's material binds the certified contract; `SubmitIntent` re-simulation inherits the same gate. `go list -deps ./cmd/hcmnext` now reaches snapshot, simassign, simcomp, simcontract and budget.

REFACTOR decision: renamed `FixtureInputs` to `CorpusInputs` (11 files plus the ARCH-GO-020 graph golden) rather than deleting — the type is the production resolver, and the corpus remains its source-data authority, so the old name's test-only implication was the lie, not the type's existence. Historical todo/evidence text naming `FixtureInputs` was left alone.

Verification on Windows/arm64: five new tests (blocked-path contract, executable-path contract via a picker-issued revision ref plus budget side proving READY to EXECUTABLE_AS_SIMULATED with the 5000 USD raise as cost, legacy path, two fail-closed refusals, proposal digest pinning) plus the full `internal/intent/app` suite green in 246s; `TestTodo_ARCH_GO_020_Golden` green after the rename; `go vet` and `gofmt` clean; `npm run check:coverage:staged` passes (nothing staged, so no packages gated). The full `internal/application` suite was still running at tick time; only its golden subset was verified green. Work left uncommitted.

# REV-006-01 verification follow-up: ALIGN regression and the application suite verdict

`TestTodo_ALIGN_027_Property` caught three slice A-C regressions after the tick: STALE manager chains admitted (known_at truncated to seconds; fixed with an RFC3339Nano cutoff), kernel baselines missing present facts (fixed with asOfFrom instant parsing plus a present-union with negatives cleared), subject revisions unpinned (fixed with a planFor fence plus revision pin). Regression tests were added and the property passes. The full `internal/intent/app` suite re-ran green (exit 0).

The `internal/application` run finished with 12 failures, every one the routed-approver credential window: the tests issue 48h credentials and verification caps the window at 24h (`MaxDevLifetime`) since the landed commit `2d864db8` "feat(trust): add machine identity authentication" (2026-09-23, another session). The files belong to ticked PROMOUX-013/014, UXAUDIT-017, WF-STEP-003 and PROMO-EXEC-001; nothing in the failure path is touched by the REV work, so they were left for the owning session. The tick evidence now records this instead of the earlier "still running".

# REV-006-02 ticked: capacity fence before the promotion commit

The RED was proven first and holds: `PositionReservationStore.Reserve` and the budget `REQUESTED->HELD` lifecycle had zero non-test callers repo-wide, and two concurrent commits for one proposal both receipted with no shared fence (disposable probe, since removed). The durable budget half turned out to be already wired (held by `internal/data/promotionbudget` at candidate materialization, verified HELD/bound/unexpired by the terminal writer); the position half had no equivalent anywhere.

GREEN, uncommitted: `localcommit.ReservationFence` port plus `PromotionReservationFence` (Acquire/AcquirePosition/AssertHeld/Release/Sweep over the real position and budget stores, digest-keyed index, replay-safe compensation that never unwinds a live hold on a changed-material replay, clock seam). `Committer.Fence` fails closed with `ErrReservationRequired`, the claim binding tenant, revision and digest. The candidate path holds the target head at materialization next to the durable budget hold: `positionHoldRequest` derives the seat from the POSITION subject at the seat's own capacity FTE, keyed to the proposal digest with a MinRevision pin and a 30-day effective-start-derived expiry mirroring the budget horizon; `holdProposalPosition` re-reads the seat through the cell reader, skips position-less revisions, and refuses reader-less cells closed. One real defect was found and fixed along the way (Acquire's budget-failure compensation could release a pre-existing hold on replay; now first-touch-only with quiet release).

Verification on Windows/arm64: `TestTodo_REV_006_02` (admit-holds-commit, replay identity, underfunded compensation frees the head), `_Race` (two promotions for the last head: exactly one commits, the other `position.ErrCompetingReservation`, the loser commit refused) and `_Security` (forged/cross-revision/cross-tenant/changed-material/expired/released) green in `internal/domains/promotion/localcommit` at 73.3% statements; `TestPromotionPositionTarget`, `TestPositionHoldRequest`, `TestHoldProposalPosition` green in `internal/intent/app`; full `go test -count=1 ./internal/intent/app/` exit 0; `go vet` and `gofmt` clean; `npm run check:coverage:staged` passes (nothing staged). Existing committer tests took an allow-fence fixture with assertions unchanged. Known limit: the in-memory fence is process-local; a cross-process position fence needs a durable hold table, which is a follow-up slice, not this one. Ticked with evidence; uncommitted.

# REV-008-01 ticked with no code changes: served TASK resume already certified

The todo was found fully implemented and served (all committed, worktree clean in those packages): `Driver.Resume` routes served TASK nodes through `checkTaskResume` when `Tasks` is set (kind check, outcome-digest binding, recorded-decision binding, submission decode + tamper digest, contract binding, `Resolve` with the schema/acknowledgement validator); the served composition sets the policy with the reapproval contract and validator; legacy submission-less completions keep drift behavior by documented design. Verified this session on Windows/arm64: `TestTodo_REV_008_01` (six subcases), `_Security` (three) and `_Integration` (real PostgreSQL open→route→claim→Submit→resume) all PASS; `go list -deps ./cmd/hcmnext ./cmd/worker` both contain `internal/workflow/steps/task`. Two deliberate non-changes recorded in the tick: the REFACTOR deletion of drift duplicates is declined (the function also serves APPROVAL nodes and every binding is pinned by `TestTodo_WF_RUN_028_Mutation`; the task package covers no envelope bindings, so the condition is unmet), and `steptask.Submit` stays mint-side (used to mint inside the integration test; journey-surface Submit recording is the documented legacy note, not this clause). A full `internal/workflow/execute` coverage run was still in flight at tick time. Ticked with evidence; uncommitted.

# REV-010-01 ticked: RULE-004 re-evaluation runs on the served execution path

The RED held: `ReevaluatePromotionApproval` had no caller outside its own engine package, and the served composition never set the driver's `Currency` port at all, so no served advance ever revalidated anything. The guard unit plus its matrix tests already existed in `internal/workflow/execute`; the missing piece was the served freeze/read wiring plus the composition, which landed uncommitted across earlier turns (migration 00328 `promotion_threshold_decision` with RLS/append-only trigger plus the disposition row, `rulethreshold` store, `Runner.RunInTx` + `RecordThresholdDecision`, `ServedRuleFacts`, served guard install).

This turn closed the remainder. Wiring the guard broke four served-composition tests: three retry harnesses use synthetic `intent:retry:*` ids the durable intent-control tables cannot key on, and the observe gate flagged the two new context-taking functions. The harnesses were deliberately built to exercise START transaction shape, not intent materialization, so retrofitting them with durable identities would test the wrong thing; instead `PromotionExecutionConfig.Currency` now permits an explicit guard override (nil keeps the strict served default for traffic) and the three harnesses run under a memory-backed `retryCurrency()` guard with a nil Rules port, re-checking the same synthetic approval the start was validated against. Two observe exemptions were recorded for `Runner.RunInTx` and `ServedRuleFacts.Lookup`, both running inside the driver's own instrumented advance. Five new tests were added: three `TestServedRuleFactsLookup*` (frozen decision resolves beside live inputs; silence without a decision or approval; nil ports and non-uuid intents refused) and two `TestRunnerRunInTx*` (freeze in EXECUTE with key/tier/row/table/digest assertions, no record on simulate/uncovered/unfreezable paths, loud error on non-uuid intents, stable PORT_FAILURE preserved, transaction claims only the threshold node).

Verification on Windows/arm64: matrix trio `go test -count=1 ./internal/workflow/execute/ -run TestTodo_REV_010_01` exit 0; threshold roundtrip/conflict green; full `go test -count=1 ./internal/platform/execution/` exit 0 at 81.5% statements, promotionsteps 79.8%, rulethreshold 73.7%, execute 83.0%; `internal/engines/rules` 63.4% sits under its valid below_floor exception; `go vet` and `gofmt` clean. Ticked with evidence; uncommitted.

# REV-003-02 ticked with no code changes: rolling-upgrade adapter already served

The RED did not reproduce: `internal/platform/schemaupgrade` has two non-test importers in the current worktree (`internal/platform/upgradejournal`, `cmd/migrate/upgrade.go`), and `go list -deps ./cmd/migrate` reaches both the protocol and the adapter. The adapter (`upgradejournal.Coordinator`) persists every lifecycle transition to a journal file and drives expand, resumable backfill, exact shadow comparison and cutover through `RunToCutover`, idempotent across re-runs; the migrate upgrade subcommand drives real version transitions through it. The REFACTOR clause holds (no os/filepath/net imports in the protocol package). Verified this session on Windows/arm64: `TestTodo_REV_003_02` plus `_Integration` (drop-and-reopen kill resume to CUTOVER, idempotent rerun), `_Recovery` (mid-backfill kill resumes to a completed cutover with appended evidence and cursor `c`), `_Fault` (missing journal refused, mutated source refused), `_Edges`, `_ShadowRefusal`, and the `cmd/migrate` `TestTodo_REV_003_02_Upgrade*` suites — all PASS. Worktree clean in those packages (committed); no Go files changed or staged by this tick.

# REV-007-04 scoping: transport boundary for analysis + surface (plan, not yet built)

RED verified: no non-test importer of `internal/intent/analysis` or `internal/intent/surface` anywhere under `internal/`, `cmd/` (only their own `_test.go` files), and no `TestTodo_REV_007_04*` exists. But the gap is narrower than the todo text suggests. Equivalence audit against the served edge/gRPC surface (`gen/go/hcmnext/intents/v1` RPCs + `internal/transport/edge/procedures.go` + journey server): `surface.Cancel`/`Supersede`/`Timeline` overlap served `CancelIntent`/`SupersedeIntent`/`ListIntentTimeline` (intent/app-backed durable writes/reads); `surface.Correct` overlaps `EditProposal`, `Escalate` overlaps `RequestJourneyIntervention`, `Inspector` overlaps `InspectJourney`/`GetIntent`. Wiring surface duplicates of those would create parallel writers and violate the single-writer layering, so they stay on their existing RPCs by design (to be recorded in the tick). The genuine unserved gap is `analysis.RecommendAction` (+ `AnalyticalRequest` handling: pure, no durable writes by design) and the surface READS with no equivalent: `DeepLink`, `Search`, `Inspector` (authority-filtered field set + digest), `Export`, `Subscribe`.

Mechanics mapped: routes follow the journey-server pattern (`trustedContext` → `requireFeatureAction/View` → engine call → convert → `ownedError`); new RPCs mean editing `schema/proto/hcmnext/journey/v1/journey_service.proto` + `buf` regen (`buf.exe` present) + server methods + edge procedure/factory entries + endpoint-manifest rows (`definitions/api/endpoint-manifest.json`, built by `internal/transport/manifest`) + gateway enablement + `invocationpath` conformance. Views for the surface reads must be projected from existing inspection (`eng.Inspect`/`ListJourneys` detail → `surface.IntentView`, authority derived from principal grants) in a thin adapter so transport stays a boundary (ARCH-GO-023); a durable `IntentView` loader is explicitly out of scope.

Planned build (next turn): proto `RecommendIntentAction` + `GetIntentDeepLink`/`SearchIntents`/`InspectIntentFields`/`ExportIntentFields`/`SubscribeIntentTransitions` RPCs on JourneyService (or the intents service if the reviewers prefer — decide at build); server methods + view-projection adapter; manifest + edge + gateway rows; `TestTodo_REV_007_04` (authz refusal, recommendation round-trip, export purpose refusal, stale-revision action refusal via existing RPCs), `_Integration` (served route against embedded PG), `_Conformance` (invocationpath manifest check green for the new routes), `_Golden` (pinned manifest digest + verdict evidence). No implementation started; worktree untouched for this todo.

# REV-007-04 ticked: analysis-to-action and inspection reads served on IntentService

Built on `IntentService` rather than the scoped `JourneyService`: `RecommendIntentAction`, `GetIntentDeepLink`, `InspectIntentFields`, `ExportIntentFields` in `schema/proto/hcmnext/intents/v1/intent_service.proto` with `buf` regen clean. `internal/intent/app/analysis_surface_endpoints.go` holds the implementation behind the app boundary: server-minted recommend auth binding (empty wire digests refused), served CurrencyGuard default with a memory-backed retry override for synthetic harnesses, and view projection from server-loaded instance state only. Transport follows the existing layers (`ports.go`, `grpcserver/services.go`, `edge/procedures.go`, typed clients plus fakes); `definitions/api/endpoint-manifest.json` regenerated (PROTO_010/API_001 now 18 endpoints) with 4 ceiling rows.

Verification on Windows/arm64: matrix `TestTodo_REV_007_04`, `_Integration`, `_Conformance`, `_Golden` plus the full `go test -count=1 ./internal/intent/app/` (exit 0); `internal/transport/manifest`, `edge`, `clients`, `transporttest` green; `go vet`/`gofmt` clean; `npm run check:coverage:staged` PASS (3 pre-existing below_floor waivers). The 4 new ceiling rows forced the designed re-sign cascade: ceiling (`6d2bfd82…`), commercial freeze re-pinned + re-signed (`f91654db…`), P1A re-bound + re-signed (`989e8876…`), P1B re-bound + re-signed (`fcf7ff32…`), all four goldens plus the `goldenP1A/B` consts, `internal/commercial.P1AManifestDigest` and the P1A evidence report regenerated; `gateevidence`, `selectionbind`, `scopeceiling`, `pilotcommercial`, `internal/commercial`, `tools/gen/openapi` suites green (the openapi regen also picked up another session's staged journey path, noted not owned). Single-writer decision recorded in the tick: Cancel/Supersede/Timeline/Correct/Escalate/Inspector stay on existing RPCs; Search/Subscribe scoped out. Ticked with evidence; the focused test matrix and implementation details are recorded in this entry.

# Chat UI Slack-parity pass (uncommitted)

A critical review of the chat against Slack found a thread pane that covered the timeline at 1024px, no way to pick a person to mention, a centred message column, search results without context, and a GIF that lost its source after a re-render. The last belongs to the concurrent session's uncommitted image-loader work and was left to it. The rest was fixed on a separate dev stack: gateway 127.0.0.1:9999, cell 9990/9743/9991, and the `hcm_next_slack` / `hcm_chat_slack` databases. That kept the pass off the other session's 8888/8380 preview and data. An adversarial critic scored headless screenshots each round at 1440, 1024, 390 and 320px, light and dark, and in German and Arabic. Scores against Slack: round 2 6/10, then 7, 7.5, 8, 8 and 7.5 (round 8 was a regression the builder caused), then 8.5 and 8.5. The final round scored build quality 9/10, and 9/10 in English (8.5 in RTL) with server work excluded. Round 10 found a real bug: the mention picker read stale state when keystrokes outran renders. It was fixed by moving the live state into a ref.

Two defects came from the harness rather than the product. Once, a visually hidden search status made a still-loading search look empty. Another time the in-app browser pane's starved timers made one-click reactions look inert; headless Chromium showed the chip updating in about 500ms. Another session's promotion workflow edit made the binary conflict with version 1.0.0 already published in the dev core database, so that database was recreated.

Left as server or feature work: posting a thread reply to the channel too (the post contract only has `parent_id`), structured mention references on send (the server has a picker API but the client's `SendMessage` carries body only), save for later, a rich-text composer, replier avatars on the thread link, and search filters.

Verification on Windows/arm64: `go test -count=1 ./internal/humanwork/chatui/` green at about 73% statements (new `slack_parity_test.go`), `go test -count=1 -run 'TestSeedLine|ChatSeed' ./cmd/migrate/` green, the productui copy and chat tests green, and the journeywasm native chat tests green. Native and js/wasm vet are clean. The full productui suite has one failure outside this change (`web161_salary_comparison_test.go`). The work is uncommitted: `render.go`, `styles.go`, `copy.go`, `chatui_test.go` and `fieldsync`-adjacent files carry the other session's uncommitted edits too, so a clean file-scoped commit needs that session to commit first.

# Chat dialogs, quiet hours, inline channel tools and mention chips (uncommitted)

Browse channels said "No channels to join yet" to someone already in every channel. It now lists joined channels alongside discoverable ones. The create dialog asked for "Names or IDs, separated by commas". It now picks people as chips and submits subject IDs. Quiet hours expanded inside the rail footer and pushed the channel list off screen; it is now a popover with a switch and a time-zone list. The to-do list and poll moved from the details pane into an inline tray under the channel header. Two client callbacks in `chat_wasm.go` no longer open the details pane, and opening the create dialog reads the directory. Mentions render as person chips that open the details pane.

The picker ran into the same reconciler trap as the first mention list. Adding a chip before the search box moved the focused node, which blurs it, and a render about 300ms later blurred it again. So `focusField` restores focus for about 45 frames while focus sits on the body. Chips are keyed so the input keeps its identity. Tray and create-dialog state live in a ref-backed store (`local_ui.go`) for the same stale-read reason as the mention state.

Verification: `go test -count=1 ./internal/humanwork/chatui/` green at 72.8% statements, with new `surfaces_test.go` and existing to-do/poll tests pointed at the tray through `renderWithTray`. Native and js/wasm vet are clean. Playwright checked the member picker end to end: focus kept after a pick, a second name typed, and Escape closes the dialog. Visual checks of the tray, poll and mention-to-person flow on the 9999 stack were blocked for a stretch because the shared tree's promotion execute workflow failed to publish with PLAN_DIGEST_MISMATCH. That comes from another session's in-progress `promotionexec` work, and the cell refuses to start until it is consistent.

REV-101-03 closed the workflow terminal-effect write-boundary gap: the effect delegates promotion settlement to `promotioncommit.Settler`, which owns guard and budget release, schema registration, and outcome ledger/checkpoint/outbox writes. The named PRIMARY/INTEGRATION/GOLDEN matrix and scoped vet passed; cmd/hcmnext imports both the effect and settlement packages. Integration verifies atomic committed rows, closed guard, and idempotent replay. This evidence covers the promotion settlement path only.

REV-102-02 keeps worker names out of durable evidence subjects and operation logs: `CreateWorker` uses the opaque worker UUID there, while the name-based slug is projected from the stored profile for worker-facing data. The PostgreSQL-backed app tests passed PRIMARY and SECURITY, including created, invalid-input-refused, and authorization-refused cases; source confirms production uses the PostgreSQL evidence store. Scoped vet passed. The name remains in the worker profile/read projection, as required for display.

REV-026-01 wires Journey `SearchKnowledge` to the knowledge domain's live lexical ranker. Trusted principal tenant and current role scope feed the query; authorization runs before ranking, and served matches carry source/policy/authorization evidence with envelope and suppression digests. The served TCP gRPC test uses a real PostgreSQL content registry and a role-snapshot fixture, proving foreign-tenant and role-restricted articles do not appear. The engine matrix and Journey PRIMARY/INTEGRATION/SECURITY matrix passed; `cmd/hcmnext`'s dependency graph includes `internal/engines/search`.

REV-084-03 connects the semantic submission registry to the served journey approval path. Identical accepted actions replay the first typed submission record; a 12-caller duplicate-decision race leaves durable approval and transition counts unchanged, and a fresh application composition reconstructs the same record from the durable accepted action. The app and PostgreSQL-backed served workflow matrices passed. The registry remains process-local; restart behavior depends on resolving the persisted acceptance facts again.

REV-030-01 wires DataOps and Integration services into the composed cell. The DataOps handler uses the application import/artifact stores, Integration resolves the durable published connector registry, and grpcserver registers both generated services. A PostgreSQL-backed running application test covers admin-only connector publication, served definition lookup, and tenant-bound StageCSV receipt; PROTO-006 exercises all 11 new unary methods through generated gRPC and Connect clients. `cmd/hcmnext` includes both service packages in its dependency graph.

I18N-004 serves an activated catalog revision from the workspace page handler. The authenticated shell resolves the tenant/product/locale revision, renders its effective text and revision diagnostics, and sends the same revision digest and message set to the Go/WASM client for scope-qualified version verification. Product UI and PostgreSQL store tests cover meaning/legal-review refusal, tenant isolation, failed activation, and recovery; served HTTP integration proves an activation changes the running page and a fresh handler recovers r2. r45 live QA confirmed the authenticated fallback and de-DE first paint; the demo tenant had no synthetic active translation, so activated-copy visual rendering is proven by the server integration rather than that live check.

# Governance and tooling repair group (2026-09-24)

Eight commits landed after `02a662bc`: `2221ea08` (GOV-030), `ff9deff8` (TOOL-006), `d53c63d7` (TOOL-026), `1645124a` (GOV-007), `c91be0f0` (GOV-015), `8c0e007b` (GOV-016), `7c886f8d` (LIB-009), and `1e494633` (TOOL-011). GOV-030 is complete: the CI workflow now runs the live crosswalk compiler/validator; local focused tests, vet, crosswalk validation, plancheck traceability, reachability and staged-coverage checks passed. No GitHub CI result was observed.

The TOOL-006 defect was that a newly added Protobuf `oneof` alternative was accepted as compatible. The checker now reports an incompatible oneof change in both directions. `go test -count=1 ./tools/gen/compatibility/` and `go vet ./tools/gen/compatibility` passed (71.1% package coverage; gofmt and diff checks clean). TOOL-006 remains open because the reachability audit found no accountable capability owner/runtime path.

The TOOL-026 defect was unbounded stale-directory reclamation and treating `EPERM` as a dead process. The sweep now preserves such live processes and removes at most eight eligible runtime directories per invocation. The focused `./internal/data/pgtest` test printed `ok`, followed by a Windows unlink cleanup warning; `go vet ./internal/data/pgtest` and gofmt passed. TOOL-026 remains open because the reachability audit did not resolve its accountable owner.

The other commits corrected multi-action todo detection, failed-evidence progress accounting, duplicate dependency-range endpoints, and container-adoption mutation coverage. Their focused package tests passed except the dependencygraph command ended with the same Windows unlink warning; its vet was canceled under host saturation. TOOL-011's focused test, vet and unreachable checks passed, while the wider quality tree remained red for unrelated TOOL-013, OIDC, rapidkit and TOOL-025 failures. LIB-009's focused test passed with `GOPROXY=off`; vet was not proven. The full pre-commit hook was skipped with user authorization, and no GitHub CI run was observed.

# Implementation-depth gate and scoped repairs (2026-09-24)

Fourteen commits landed after `128a16d3`: `32349b6f`, `47eed38d`, `cd847ec1`, `bf2132c3`, `778287ad`, `c802d39a`, `b648ecb2`, `b0c1deaa`, `29a5c94b`, `ac362cb3`, `0e4bf776`, `6ee858c4`, `d15ca0d8`, and `a25a47ce`. GOV-005 is complete. The owner is Platform Engineering; CI now runs the `depthvocab` package and live `plancheck depthvocab` command. Locally, the package reported `ok`, the CLI printed `depthvocab: OK`, and traceability/reachability passed. Windows then denied cleanup of the temporary executables; per `AGENTS.md`, the successful Go result lines are authoritative. No CI run was observed.

The group also adds planning depth and production-import/terminology checks; repairs the Windows OIDC/fuzz cleanup, rapidkit artifact scan, orphaned-quarantine check, and module inventory; adds the recovery policy-matrix CLI, historical retired-suite verification, and generated WASM asset ceilings; tightens architecture-signature, dependency classification, and exact workspace exemption checks. Scoped verification: TOOL-025 toolinventory test/vet passed; oidckit test and vet passed (post-pass unlink warning); rapidkit test/vet passed; TOOL-013 fuzz classification and subtests plus vet passed (post-pass unlink warning); GOV-020 focused regression and vet passed, while its full package failed on 19 unrelated unquarantined skips; RECOVERY-001 hcmctl/recovery/opcmd tests and vet passed (post-pass unlink warning); CRYPTO-001 focused test, gofmt, and diff check passed; ARCH-GO-027 focused test printed `ok`, with full package/vet unverified after cache errors; WEB-236 asset-budget tests passed; LIB-001 depmanifest test printed `ok` with vet, gofmt, and diff checks passing (post-pass unlink warning); GOV-009/012 focused checker and plancheck adapter tests passed, the live CLI checks printed `OK` with exit 0, and all three vet checks passed; TOOL-001 focused test printed `ok` with vet/gofmt/diff checks passing (post-pass unlink warning). The GOV-020 full-package failure and ARCH-GO-027 verification limit remain as stated.

FEATURE-001's focused `intentmanifests` test, vet, gofmt and diff checks passed, with traceability already passing; it remains open because its digest is self-computed and its authoritative source qualifiers/examples are missing. These changes leave partial work open: GOV-007, GOV-009, GOV-012 and GOV-020 still need their complete closure conditions; GOV-016's live dependency scan has 6,202 phase-inversion findings; TOOL-001 still lacks owner/complete-gate closure; LIB-001's full qualification and Testcontainers workload execution are unproven; RECOVERY-001 remains outside serving-binary dependency closure; CRYPTO-001 still lacks the production custody path and governed suite policy; WEB-236 still lacks the service-backed page; ARCH-GO-027 remains open under its owner disposition. The full pre-commit hook was skipped with user authorization. No CI result is claimed.
