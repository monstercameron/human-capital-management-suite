# Promotion PR merge-gate repair — 2026-09-21

## Chat agent API documentation — 2026-09-22

The README now records the installed-agent HTTP routes available for public channels, private channels, group conversations, and direct conversations: metadata, newest-first paged posts, idempotent post creation, and bounded post-event pulls. The local example is explicitly anchored at port 8888 and uses the opaque conversation ID supplied by the channel's Copy API curl action.

The access contract is a verified short-lived `Agent` or `Integration` bearer identity whose tenant and subject match a manager-approved installation in that conversation. `chat.posts.read` covers metadata, post reads, and event pulls; `chat.posts.write` gates post creation in addition to current conversation policy. Event continuations use the returned opaque, signed, expiring `resume_cursor`; the HTTP API rejects `after_sequence`. The development `hcmnext token` command can mint a short-lived development agent token, but it does not establish an installation or issue production credentials.

The real-PostgreSQL four-kind authorization and read/write matrix passed three runs. Coverage includes public channels, private channels, group conversations, and direct conversations; the 5,000-row regression confirms installation-time filtering happens before the event-page limit. Additional checks confirmed a transient database failure returns 503 and revoked/private resources use indistinguishable 404 responses. On r9, port 8888 served 48,591,082 bytes of WASM with SHA-256 `FC9713D57208D29A2CCE96059CA0D0E0374703B7CC7820B807F6253500063350` (HCM PID 53720, gateway PID 55708); the app, WASM, and health endpoint returned 200. Browser QA opened the API menu for all four conversation kinds and copied the updated curl snippet; the console was clean. No chat todo was marked complete, and no files were staged or committed.

## Chat global search — 2026-09-22

Chat search now returns authorized channel-name matches, visible people, and matching messages. Each category pages independently; selecting a message navigates to and focuses its exact timeline article. A browser pass on r5 exposed a fatal WASM `Illegal invocation` because `requestAnimationFrame` was detached from its window receiver. The focus helper now calls it bound to the global object, and the focused Node-executed JS/WASM regression passed.

On r6, independent QA exposed a cache race: a loaded People projection overwrote the People result from a fresh search, so “Anika” returned seven messages but no person row. `adoptLoadedChatProjection` now merges the arriving projection without discarding the in-flight search result, and a regression covers the interleaving. On final r7, port 8888 served a 48,506,351-byte WASM (SHA-256 `61872873D55DC558D19AA1E8E75176A4C6E4C26F6BA8EE953E37A20C1915CD1C`; HCM PID 59348, gateway PID 55708); app, assets, and health returned 200. Root and an independent fresh browser both confirmed “Anika” displayed People → Anika Desai plus seven messages (eight results total), with a clean console.

Final independent r7 visual QA scored 9/10 with no P1/P2 findings. In a fresh session, People → Anika and seven messages appeared together; `bene` returned #benefits; selecting a message focused its exact article; and the post-jump layout remained responsive. Search and results were readable at 390px and 320px, with an empty console. The viewport was reset and the QA tab closed. Earlier interaction checks also confirmed message paging from 50 to 100 and Escape clearing search. Focused native and JS/WASM search/focus tests passed, including the bound-`requestAnimationFrame` `FocusSearchMessage` regression and the r7 projection-interleaving regression. CHAT-021 remains unchecked because its dependencies and full todo acceptance matrix are still outstanding. No todo was marked complete, and no files were staged or committed for this search change.

## Chat capability and rendering follow-up — 2026-09-22

Added safe Markdown rendering to timeline messages and threads, accessible emoji pickers to the main and thread composers, accessible skeleton loaders, draft merge/retry behavior for concurrent sidebar updates, and self-DM support. Uploaded PNG/JPEG/BMP files now have protected 640 px thumbnail and 2560 px display renditions, with near-viewport loading and a fullscreen viewer that requests the display rendition. Original GIF bytes remain unchanged. Per-channel integration cURL examples accompany scoped machine-authenticated post read/write routes with idempotency, and tenant admins can save retention age/date or size settings.

Focused native `chatui` and `productui` checks, focused executed JS/WASM `chatui` and `journeywasm` checks, native and WASM builds, narrow vet, and real PostgreSQL machine API HTTP integration passed. The image backend checks `go test -count=1 ./internal/collaboration/chatmedia/ ./internal/transport/chatmedia/` passed. Final candidate r4 ran on port 8888 as HCM PID 88108 and served WASM SHA-256 `A161203B351EBA2C9FA6ADDA07EAC818692021B110D2B8A4AF9A32D313BED1F9`. Browser review confirmed the image thumbnail and fullscreen viewer, Rafael's business profile and Message action, and the self-DM label after reload. A native and executed WASM regression confirmed replayed reaction-count idempotence, and self-DM member copy is localized and singular. The no-key GIPHY composer displayed its disabled explanatory state; no GIPHY search was run. The #general API cURL action copied the current-origin URL with a token placeholder and UUID idempotency header. No chat post or destructive retention action was used for QA.

Known limits: retention policy is persisted but not enforced while copy inventory is incomplete; production machine credential issuance and event replay are pending; GIPHY search needs a configured API key; animated GIFs are not optimized; and self-DM lookup has a scalability gap. Final independent Chrome adversarial QA on r4 scored 9.0/10 with no P1/P2 findings. As Rafael, 33 reaction counts remained 1 while opening and closing the fullscreen viewer, jumping to newest, and switching #announcements → #benefits → #announcements; the self-DM header showed “1 member” and the console stayed free of warnings and errors. Root IAB independently observed 12 visible reaction counts stable during viewer and jump interactions. The todo audit found no CHAT item eligible to tick: required dependencies, acceptance matrices, scope/pilot evidence, or release gates remain outstanding, including CHAT-001's unapproved scope exchange. The full shared-index coverage and commit hook were not run, no files were staged or committed, and this feature QA does not represent a full repository or CI pass.

## Chat adversarial interaction refinement — 2026-09-22

Two independent reviews scored the final interaction surface 9.25/10 with no release blockers after the 7.5/10 baseline. Message and thread menus now restore the actual opener through arrows, Home/End, Escape, Tab and reverse Tab. Late projection updates preserve an open five-reply thread and loading state. Mobile drawer search focus, focus trapping, inert background and cleanup were driven through close paths. Menu geometry flips, caps and repositions from measured bounds; representative 390 px light and 320 px dark Arabic checks stayed within the viewport. Root directly verified final D6EC behavior and an empty console.

R5 fixes the async thread loading race with a framework `ui.PostAsync` one-lifecycle tick fenced by mount epoch, generation, room and thread root; it does not use timers or router fallbacks. The real DOM `TestChatControlsPreserveTimelineDOM` regression queued the framework update, mounted the chat, committed one synthetic reply, asserted its reply text without another action, and covered unmount. Root independently executed the projection race; live port 8868 health returned 200 and served the exact promoted 47,522,155-byte WASM, SHA-256 `F5E869FD11F318AD0FB75B0399F2202D9892D2E5E8241BA78AA487F69774B8CC`. A fresh live Q4 browser smoke showed all five replies after one click and wait, without another action or loading state, with an empty console; the 0.365-second wait is observational UI timing, not a benchmark. Focused native chat UI, executed WASM menu/drawer, native journey/thread/composer tests, and vet passed. One reviewer could not authenticate the final runtime; the other reviewed source and screenshots without independent browser, screen-reader or contrast instrumentation. Remaining minor notes are Tab returning to the opener before a second Tab and dense 320 px header spacing. The shared-index full hook/coverage remains unrun; the unrelated `TestTodo_WEB_148` product UI failure persists. No staging or commit was performed by this lane.

## Copy channel and chat references — 2026-09-22

Conversation rail menus gained Copy name and link. The clipboard writes `#Name_With_Underscores` and an absolute same-origin `/workspace/app/chat#channel=<escaped ID>` URL as both `text/plain` and a rich HTML anchor where supported; `writeText` is the fallback. Display names in the rail are unchanged. A copied URL pasted into a chat draft, timeline post, or thread reply renders as a safe clickable reference. Known joined rooms select in place. Unknown room IDs retain a generic `#channel` link and go through the authenticated `GetConversation` receiver; links do not add membership. Cross-origin lookalikes and malformed fragments are refused, delayed reads are guarded against session changes, and clipboard notices use English, German, or Arabic copy. Native `go test -count=1 ./internal/humanwork/chatui`, executed JS/WASM `^TestChat(ConversationReference|ChannelFragment|ChannelReference)`, and narrow vet passed on the joint source. Preview QA copied `#general`, `#Q4_hiring_huddle`, and `#Evelyn_Morgan`, clicked a pasted General draft preview to open General, and opened a fresh copied URL in a new tab directly into General. Menus fit within 53–253px at 320px and 101–301px at 390px, with no horizontal overflow or console errors. Live port 8868 served the promoted 47,443,945-byte WASM with SHA-256 `F72EC3971F28F69997887F0F080401F5A9C25D0782BC03AA2EADF52A5110E35B`. A live clipboard read confirmed `text/plain` as `#general` plus the 8868 URL and `text/html` as the labeled anchor; Ctrl+V pasted the plain form. Drafts and clipboard were restored after QA, temporary QA tabs were closed, and no post was sent. The full shared-index hook and coverage remain unverified; the shared index was untouched.

## Chat timeline memory and media bounds — 2026-09-22

The chat timeline now activates variable-height virtualization above 160 messages, capping the active window at 96 rows plus four protected controls; lists at or below 160 messages render normally. The 2,000-message fixture rendered 17–29 DOM rows at 1280 px; append and prepend preserved the exact visible ID and offset. Editor caret/focus behavior, offscreen parking, and clean unmount/remount were verified, with no console diagnostics at 390 px or 320 px. Two-sided paging handles reply-only scan pages, retains seen IDs with a bounded 128-row overlap, prunes reaction cache state, and preserves drafts and other intentional user state. Pin revisions scale with active server pins and directory/organization state.

Media preview bounds are 16 MiB encoded and 32 MiB estimated decoded for PNG/JPEG/GIF, with four fetches, 20-second aborts, lifecycle cleanup, epoch-based late URL revocation, and viewport-only rendering while preserving fullscreen. Other or oversized media use bounded downloads up to the 64 MiB server limit, with one active and two completed retained URLs for no more than 30 seconds; room, auth and unmount clear them. Thread snapshots retain one root plus updates/deletes/media. Production `image/gif` rendering was verified with `celebration.gif` at natural width 120; separately, its download path saved a 2,271-byte file (the browser download event itself timed out).

Verified: root native chat UI full pass; focused journey tests for 10,200 events, 500 timeline rows, bounded-seen paging recovery, and media lifecycle; combined executed WASM media, controls, embed, share and person tests; native vet for both packages; and executed focus/selection checks. The 2,000-message stress run held Jump-to-newest at a 0 to −1 px bottom gap and retained a 71–72 px small up-scroll through rerender, prepend and append; the final app preview/live smoke measured 0.33 px and 72.33 px. Exact row/offset anchors survived prepend 100 and append 100 while away, and re-jump returned to bottom. Home → Chat remounted cleanly with an empty console. The broad WASM regex initially exposed nil-DOM assumptions in native SSR tests; a test-only native build-tag adjustment fixed it and the broad regex now passes. Live port 8868 health returned 200 and served the exact promoted 47,331,035-byte WASM candidate, SHA-256 `3F9E85417D7BF12318ECB3768E6BCF7925D91CA3D44657220D9F1A6CDA894ADE`. The shared-index full hook and coverage remain unrun under the separate coordinator; the full product UI suite has an unrelated `TestTodo_WEB_148` failure while focused chat verification passed. No staging or commit was performed by this lane.

## Chat group creation controls — 2026-09-22

The conversation rail now presents group creation as a full-width native disclosure instead of a permanently visible squeezed textbox. The expanded form has localized label, input, Cancel and Create actions. Enter creates; Cancel and Escape clear and close the form and restore summary focus with `preventScroll`. Opening the disclosure measures the form against the nearest `.rail-scroll` viewport and adjusts only that scrollport so the controls stay visible; it does not move the document or timeline. Draft text survives unrelated room changes. The missing plus SVG path was added.

Native chat UI tests and the focused executed JS/WASM disclosure regression passed. Browser QA passed in dark mode at 1280×720, 390×844 and 320×740: the form stayed visible, the plus icon rendered correctly, document scroll remained zero, no horizontal overflow appeared, and there were no console warnings or errors. Light mode was not inspected. No staging or commit was performed by this lane; promotion coordination remains with the root lane.

## Chat profile photos — 2026-09-22

Chat now carries authorized `ProfilePhotoUrl` values from the worker directory, with the existing People projection as a fallback. It indexes worker refs and IDs explicitly, resolves DM peers only from admitted memberships, and retains fixed-size initials beneath images for absent or failed photos. A browser diagnostic exposed a deferred `img` panic: handlers wrapped at package initialization remained raw Go functions. Moving their one-time wrapping to first render fixed the mount. Native chat UI and journey WASM tests, vet, JS/WASM cold-route reconciliation with photo and attachment images, and failed-then-loaded image recovery passed. The clean local desktop dark browser showed matching People photos for Anika, Zuri, Samuel, and Rafael; Evelyn's DM avatar, four member photos, and five thread photos loaded, Ethan retained initials, and there were no console warnings or errors. The emoji picker beside Samuel left row height and top unchanged. At 390×844 and 320×740 dark viewports, inner and scroll widths matched and photo slots stayed 34.67 px. The same bundle was promoted to the live runtime; its chat restored with Mina and Camila photos matching People, an MR initials fallback, and no console warnings or errors. Light mode was not inspected. No staging or commit was performed by this lane; another session owns that coordination.

## Bounded notification storage/read API — NAAS-003

Added reusable recipient-scoped ListPage and WorkflowNoticesPage methods. Shared validation bounds pages to 200 rows (50 default), applies read/archive/pin/date filters and uses stable descending creation-time/UUID seek keys. Workflow reads also filter purpose and instance; the existing bounded notification reader delegates to the new query. No COUNT, OFFSET, full-history allocation or authorization cache was introduced. Tests cover equal-timestamp traversal, archive/read/pin combinations, wrong tenant/recipient, invalid inputs, cancellation/error propagation and cursor cleanup. No partial page escapes a database failure. Existing publication, rollback and CAS behavior is retained.

Migration 00321 adds three recipient-feed indexes and one workflow-message lookup index. The old index ordered state-change time rather than creation time. A disposable PostgreSQL schema with 100,001 inbox/message/recipient rows measured 50-row p95 at 82.2ms without the feed indexes, 0.76ms indexed first page, 0.71ms deep page and about 0.55ms unread/pinned. The deep unread EXPLAIN used the intended index, returned 51 rows and touched five cached blocks without sort/sequence scan. One joined workflow query was 10.7ms. These warm single-recipient storage figures exclude transport and current-workflow authorization; homogeneous workflow metadata does not qualify sparse joined filters or sustained production load.

Final full inbox suite PASS at 83.3%; vet and source-style checks PASS. Targeted journey notification and service-descriptor regressions PASS (Windows subsequently reported an executable cleanup access-denied, not a test assertion failure). The composed bootstrap test remains blocked by another task's removed edge.IntentClient/RegistryClient APIs; no unrelated harness repairs. No new UI surface, browser claim, live index deployment or service restart. Index rollout is non-concurrent and needs a rehearsed maintenance window on populated cells. NAAS-004 records the remaining independent gRPC/HTTP API and client wiring; NAAS-005 records mixed-tenant million-row capacity/rollout qualification. The public API work was not left as an unimplemented generated RPC.

No staging or commit: another task's backend changes and overlapping planning files already occupy the shared index. NAAS-003 remains open for composed gate/commit closure rather than being falsely marked shipped.

Final checks: `npm run test` PASS, `npm run check:coverage:staged` PASS for the two already-staged packages (not this unstaged inbox work), and explicit inbox coverage above. Added final assertions for the maximum-size unpinned/unread page and a nonexistent workflow filter; targeted test PASS. Registry validation caught an earlier NAAS-002 matrix entry combining two test names; split it into independently keyed SECURITY and MANAGER_SCOPE entries before regenerating.

## Approval-aware queue handoff — NAAS-002

Three red scheduling regressions reproduced premature PARKED returns when an approval and independent READY continuations coexist. Shared dispatch now drains eligible work before parking across approval, task, signal and timer entry points. A drained run checks the durable versioned frontier rather than mistaking a waiting sibling for lost progress. Pending approvals still block their own successors. Recovery exercises RedeliverReady after a vote commits but the next step fails; replay records no second completion. Rejection follows only the rejected route, and revoked authority, wrong tenants and stale versions fail closed.

Live Codex-browser verification found a separate manager handoff failure. Safe correlated phase and error-code logging isolated an organization-scope mismatch: the request was scoped to People Ops, but its directly assigned manager worked in Sales. The journey now admits that case only after current-manager authority is read from durable facts, with the credential, tenant, recorded candidate, authority term and separation checks retained. The same check runs inside the kernel vote transaction using its freshly loaded candidate. Finance, delegated and configured-fallback approvals receive no cross-scope exception. Authority refusals now map to denial rather than an anonymous internal failure.

Verified the synthetic Adrian promotion as Camila: the notification opened the exact request, manager approval committed, the workflow reached WAITING_EFFECTIVE_DATE, the action count fell from one to zero and the notification said “Your decision is recorded.” Employee pay was not changed. The correlated successful decision was req:2eb9cab630cf9d3a / req:4658cf0f021e702d. The dev server remains at 8096 with the scheduler disabled as before; this live test proves immediate decision handoff, not autonomous timer firing. Recovery/timer behavior is covered by the separate PostgreSQL suites.

Checks: execution package PASS (83.5%); app package PASS (60.2%, existing exact-path coverage exception through 2026-12-31); promotion completion/restart/duplicate timer-and-resume E2E tests PASS; vet for both edited packages and code-style PASS. No root module sweep or local race detector. Desktop and 390px dark notification/result states were inspected. At 320px the existing popover can clip on its left edge; this UI defect and the modal's blank accessibility labels remain open under NAAS-001 rather than being misrepresented as passed. Restored the viewport after checking. Full bootstrap tests still cannot compile while the other session removes legacy edge clients. No staging or commit: the index already contains unrelated execution/cancellation edits plus overlapping planning files. NAAS-002 remains open for final gate closure, not missing queue implementation.

Final check results for the queue handoff: `npm run test` PASS (component/workspace suites); `npm run check:coverage:staged` PASS for the two already-staged packages, not a claim that this session's unstaged changes were covered by that command. Direct full-package coverage above covers this session's engine/app changes. TODO registry regenerated at 2235 entries and `git diff --check` PASS.

## Initial NaaS approval inbox — NAAS-001 (verification still open)

Connected workflow work-item routing to the existing audience resolver, messaging metadata and secure inbox. Publication shares the caller's transaction and uses deterministic notice IDs plus transaction locks. Endpoint reuse fixes the unique endpoint-digest failure exposed by the integration test. Conflicting replay is refused; replay does not reset read state. References and correlation are stored rather than compensation or message-body content. Readers require the exact recipient, current assignment and current authorized journey visibility. Batched work-item reads avoid an N+1 query path; results are bounded to the newest 100 notices.

The existing journey gRPC response carries the inbox projection, and the shared notification component links to the exact approval with its current state. A read failure reports unavailability without losing the work list. Localized empty, failure, pending, cancelled, expired, unknown and recorded-decision states are covered. The theme tests caught an undefined accent-soft token and an accent-colored keyboard ring; both now use existing theme/focus tokens. Three chrome goldens were updated for the new empty inbox paragraph before Open My Work.

Manual computer-use verification on 127.0.0.1:8096 used real seeded local accounts: Rafael proposed Adrian's promotion, started it, and Thomas received the finance notification. Opening it reached Adrian's exact approval. Recording the finance decision advanced the workflow to manager review, changed Thomas's action count to zero and retained the original notification with completed status. No employee ledger write was made by reading a notice. A separate Linh attempt was correctly refused because her manager was also the requester; the generic refusal copy remains an existing UX issue, not an authorization bypass. The publication log includes tenant, work-item, request and correlation IDs. Dark notification layouts were inspected at desktop, 390px and 320px; the integrated People chooser at those widths and in the unsaved light preview. No organization appearance changes were saved.

Passing suites: productui 92.3%, inbox 75.2%, journey transport 84.6%, productclient 87.1%, intent/app 60.3% (existing exact below-floor exception). Targeted execution publication and authorization tests also pass. The new composed bootstrap regression cannot compile while another task removes edge.IntentClient/RegistryClient without migrating its test harness; a later full execution run also caught a concurrent undefined stripCompensationVersion helper. These unrelated edits were not changed. NAAS-001 remains open until composed verification is green; no full-hook or CI claim and no commit while another task's changes occupy the shared index.

Scope limits: new routed items only, no historical backfill or reassignment publisher, no mark-read/archive controls, no external email/SMS provider. The broader REV-011 messaging obligations remain open. The live synthetic Adrian request is now awaiting its manager; it was not advanced to the business ledger.

Final refinement found and reproduced a warm-navigation regression: switching to Settings discarded the shell's notification projection. The loader now retains notices only with the same admitted session baseline, replaces rather than appends on refresh, and clears them on a failed authorization/read refresh. TestWorkflowNotificationsSurviveNavigationAndRefreshSafely covers all of those states and nil wire entries; the full productclient suite passes at 87.5%. Four concurrent publishers yield one TASK notice in embedded PostgreSQL, conflicting replay fails, and rollback leaves no orphan message intent. Routing/publication tests assert tenant/correlation attributes and exactly one telemetry completion. The final decision label is "Your decision is recorded", not whole-workflow completion.

The transient missing compensation helper was subsequently supplied by the concurrent task and the application builds again. The broader execution package still reported unrelated compensation instrumentation and promotion-step composition failures (80.8% measured coverage is not a passing suite). Our own decorator's missing outer telemetry operation, caught by that scan, was fixed and regression-tested. Bootstrap remains blocked by its removed legacy edge-client references. The dev server was rebuilt on 8096 without stopping any other service.

Final browser retest on the rebuilt server: Home → Settings retains Adrian's notice, the recorded-decision copy fits at 390px and 320px, and its link opens the same journey with Finance completed and Manager approval current. Viewport restored; the browser remains signed in as demo finance reviewer Thomas with that journey open. The final targeted publication, projector and navigation tests pass, as do vet, build, formatting and diff checks. No shared-index changes or commit were made.

### People search layout follow-up

Moved the reusable column chooser inside the Find an employee section without nesting forms. It shares the search surface, expands below the filters, preserves the visible search label on wide screens and constrains inputs at narrow widths. TestPeopleColumnsBelongToSearchPanel pins the composition; mobile screenshots verified the previous overflow is gone.

## People directory column configuration — UICOLS-001

Added one shared column chooser and an allowlisted directory-column catalogue, extending the existing table and immutable search/sort index rather than duplicating the table. Person remains visible; worker number, job code, level, company, business unit and cost center are optional. Column IDs use the existing per-principal gRPC table-preferences map (`people.filters.columns`), with no schema change or browser-local preference store. Explicit URL choices override saved defaults. Apply preserves the current filters and page size, and hiding the active sort resets it to name.

Browser refinement caught a light surface leaking into dark mode, lost unsaved checkbox choices on live-data outlet remounts, and missing column/filter state on generated links. These were corrected with resolved theme aliases, application-owned ephemeral chooser draft state, and explicit navigation state; regressions cover each. The live Go/WASM server is rebuilt on port 8096, retaining the existing database and disabling migrations/scheduler during the restart.

Verified full productui tests (92.2% coverage), productclient tests (87.3%), focused tests after the final navigation patch, route/focus tests, vet and the code-style gate. The first full run caught the catalog-key audit omitting extension catalogs; its key list now comes from the same assembled catalogue as the coverage report. Another concurrent workflow-designer test initially failed and subsequently passed without editing that test. Windows Go occasionally reports executable cleanup access-denied after a successful package result; this is recorded separately from assertion failures. The 10,000-row indexed sort benchmark was about 6.8ms locally.

Manual Codex-browser verification: desktop dark mode, 390px and 320px, keyboard disclosure/checkbox/Apply, selectable Job code and level, Job code sorting, cost-center search (5 of 60), reset preserving the search and resetting hidden sort, and restoration from server preferences after a full reload without columns in the URL. Light mode and de-DE/ar were not visually inspected; localized component rendering is tested. No commit: the shared index already contains another task's backend changes and overlapping planning files. Those staged changes are untouched; no full commit-hook/CI claim is made.

PR #55 is not yet merge-ready. The first sharded CI run on `8720e7cf` revealed source-policy drift in addition to a transient Docker Hub pull timeout and PostgreSQL termination in a race shard. The fan-in remained red; no bypass or merge was attempted.

This pass fixed the external serve shutdown deadlock (the `Serve` workloads ignored cancellation while bootstrap waited for workload drain before running shutdown steps), the invalidation readiness race, the safety store's bare-connection-capable port, direct transport imports of ledger and workflow-draft stores, and missing workflow-designer telemetry. It also refreshed the endpoint manifest and bounded planning/architecture goldens and corrected platform-specific or stale tests. Focused tests include `test/serve`, workflow designer and observation, `internal/transport/admin`, `internal/transport/cell`, `internal/intent/app`, and import/store boundary policies.

The California profile's source pin was updated to the current rule pack and re-signed using the documented repository-local development fixture. Its dependent P1A and P1B artifacts were re-signed in sequence, and the commercial identity, reports and exact golden digests were reconciled. This does not invent a legal reviewer or make the still-UNREVIEWED jurisdiction releasable. The selection binding and signature suites pass.

The referenced foreign-key audit now has a migration for its uncovered child keys and pins the current post-migration state, while retaining the intentional omissions. The x/text parser is again hidden behind an owned value seam, and the conformance scan excludes ignored local worktrees. Conditional test skips and evidence-freshness gaps gained exact, expiring records; those are accountable debt, not a claim of fresh proof. The design corpus images that linked planning pages require on a clean checkout are staged as source assets.

The local staged-coverage run exercised 33 changed Go packages and found one Windows-only false failure in the P1A evidence compiler's nested `go test`: it judged the process exit rather than an exact named test/package result after the toolchain could not unlink its already-passing binary. The compiler now reads structured Go test events, refuses a no-match or failed test, and accepts only the narrow post-pass Windows cleanup case. The focused real subprocess and classification tests pass; staged coverage still needs a fresh full pass.

The previous Linux race shard's 30-minute timeout was traced from its goroutine dump to concurrent journey-note retries: a found idempotency key retained its read transaction while author-name resolution requested another pooled connection. Releasing that transaction before resolution removes the pool-starvation cycle, and the eight-concurrent-submission integration test passes three consecutive local runs with bounded contexts. The interrupted pre-commit hook made no commit; the complete hook must run again with this fix staged.

The next unmodified hook passed staged coverage for all 33 changed packages, then stopped at the source drift gate: migration 00320 had not been copied into the generated storage manifest, and the Go dependency graph had changed without its generated architecture document. Both derived files were regenerated and their diffs reviewed (one migration listing, two new module edges); the nine-check drift gate and API/substrate/engine checks now pass locally. A full hook rerun remains required.

Remaining CI findings and any transient PostgreSQL or network failures must be resolved or rerun before merge. No gate bypass or merge has been attempted; this file records progress, not gate closure.

## CI reconciliation after `af39f238` and `9d44ab5f`

Both commits passed the complete local pre-commit hook. The first Linux matrix exposed stale PROTO-006 disposition expectations, a generated client, missing ignored-but-linked design assets, and journey-note pool starvation under concurrent projection. The follow-up closed read transactions before nested simulation/directory reads, bounded its fixture pool to four connections, restored the referenced assets, and passed repeated focused tests. An exact, expiring coverage exception records the planning command's 38.8% floor debt rather than claiming it is covered.

The next Linux matrix exposed a more fundamental test-isolation defect: `TestTodo_WF_RUN_022_Fault` sent `pg_terminate_backend` to **every client in the shared CI database**, killing unrelated schema-isolated packages and causing repeated SQLSTATE 57P01 failures. Fault injection now labels the fixture pool with its unique schema and terminates only that label; the fault test holds a guaranteed victim session and checks a same-database bystander retains its PID. The PostgreSQL log-on-failure step made this cross-package effect visible.

Three independent test-oracle mistakes were repaired without weakening production invariants: a partition fixture had returned two tenants from the _same_ hash partition, the repair concurrency test rejected the executor's documented safe `INDETERMINATE/UNKNOWN` during an unsettled claim, and the promotion stream reconnect supplied a manager's projection digest for a proposer's stream. Focused verification: partition proof five runs, repair race fifty runs, served promotion regression twice, and WF-RUN-022 recovery/fault suite green locally. The full hook and fresh Linux matrix are still required before merge; this section makes no green-CI or merged claim.

## CI planner-version alignment

The next run passed root quality, embedded PostgreSQL, coverage shards 0/2/3 and race shard 3, but coverage shard 1 reported a `DB-020` query-plan golden mismatch. Its CI service was PostgreSQL 16 while the authoritative `pgtest` embedded server and its plan golden use PostgreSQL 17. The golden test passes four consecutive local runs on embedded 17. The three CI services now use PostgreSQL 17 as well, retaining the exact plan-shape check rather than weakening its assertion.

The following Linux run passed the quality gate and coverage shards 0/2/3 but coverage shard 1 exposed a separate isolation bug: `DB-002` selected a `pg_constraint` by name across **all** concurrently migrated test schemas. When another package dropped its schema between OID selection and `pg_get_constraintdef`, PostgreSQL returned XX000 (relation OID vanished). The integration query now joins relation and namespace and selects only `current_schema()`'s `ledger_stream` constraint, as the other schema-vocabulary tests already do. This finding is independent of the planner-version alignment; both corrections need a fresh matrix before merge.

## Workflow engine adversarial hardening: WF-RUN-041, WF-RUN-042 and OBS-025

The review found and reproduced two execution defects. A valid lease was not necessarily bound to the instance being advanced, so a foreign instance or queue lease could pass verification. Binding checks now run at the runtime boundary and before driver steps, including returned acquisition grants. Explicitly supplied empty fences are refused instead of silently treated as absent. A second PostgreSQL regression proved that a superseded proposal could commit its earlier transactional step writes together with the BLOCKED status. A savepoint now discards input and retry-preparation writes before persisting that block decision; savepoint faults roll back the entire advancement.

The telemetry review found four observable failures: exported operation spans lost engine identifiers whose names did not match the canonical attribute policy; identifiers learned at completion reached logs but not spans; durable resumes recorded the preceding dispatcher trace rather than the active resume trace; and a shadowed error variable could close a failed cyclic-resume lookup as SUCCESS. These are fixed, with exporter and regression tests. Storage failures or context cancellation beneath refusal wrappers are now classified as failures. Final attributes still pass through the existing export filter, business correlation is preserved, and error message text is not exported.

Verification on local main: the full runtime and execution packages pass at 85.5% and 82.2% coverage; platform execution passes at 80.2%; workflow observe, OTel and provider telemetry pass at 96.8%, 73.7% and 97.8%. Commands were `go test -count=1 -cover ./internal/workflow/runtime/`, `go test -count=1 -cover ./internal/workflow/execute/`, `go test -count=1 -cover ./internal/platform/execution/`, and `go test -count=1 -cover ./internal/workflow/observe/ ./internal/platform/telemetry/otel/ ./internal/connectivity/providertelemetry/`. The compiler, frontier and all step packages pass with `go test -p 1 -count=1 ./internal/workflow/ ./internal/workflow/frontier/ ./internal/workflow/steps/...`. The complete execution suite includes the new PostgreSQL rollback and savepoint fault tests; added acquired-fence and correlated-refusal checks also pass individually.

The composed observability test passes with `go test -count=1 -run '^TestWorkflowEngineOperationsAreObservableEndToEnd$' ./internal/application/`. Promotion completion, manager rejection, revalidation blocking, restart and duplicate timer/resume tests plus OBS-023/024 integration pass with `go test -count=1 -run '^TestPromotionWorkflow(CompletesEndToEnd|RejectedAtManagerApprovalWritesNoFact|SurvivesRestartDuringApprovalAndWait|DuplicateTimerFireAndDuplicateResumeAreIdempotent|RevalidationBlocksEndsBlocked)$|^TestTodo_OBS_02[34]_Integration$' ./test/workflow/`. Authority refusal/admission, lost-commit resolution and execution-evidence bootstrap checks pass with `go test -count=1 -run '^TestExecuteIntent(IsRefusedWithoutExecutionAuthority|RunsThePromotionDriverUnderAuthority)$|^TestTodo_DBEDGE003_|^TestTodo_OBS_024_Integration$' ./test/bootstrap/`.

Changed packages pass go vet and formatting; the code-style gate passes. Several successful Go runs ended with the known Windows executable-unlink error after an explicit package PASS; those are recorded as package passes, not clean process exits. The staged coverage command found no staged packages; the explicit coverage runs above supply the coverage evidence. The repository-wide Go gate is blocked by `internal/humanwork/productui/wfui009_outline_test.go:21` referencing undefined `workflowDraftWorkspace` in another session's editor work. No commit was attempted and no shared-index contents were changed. Linux race/CI, live Collector export and production load/soak remain unverified; this pass does not certify the entire engine as production ready or close the other production-integration backlog.

## Parallel and join building-block hardening (WF-STEP-021/022)

Reviewed the bounded PARALLEL and JOIN building blocks, their step contracts, callers and existing tests. The unchanged baseline passed at 85.2% coverage but accepted duplicate branch evidence across every join strategy. Two copies of one successful branch could satisfy a two-branch quorum; empty branch IDs were also counted. Required-set evaluation stopped at its first non-success, hiding later missing members and making known-failure versus unknown verdicts depend on order. The fix validates unique nonblank result/required IDs, checks the whole required set, and uses deterministic failure precedence while retaining the unknown list. Existing valid golden outputs were not changed.

Parallel admission accumulated signed integer costs without overflow protection. New tests reproduced maximum-integer plus one, maximum plus maximum, and negative-budget overflow starting work. Admission now uses subtraction from a validated nonnegative remaining budget and refuses before starting branches. Exact maximum-integer totals and zero-cost branches remain supported. The fault regression verifies one ended non-success telemetry operation and no branch operations on budget refusal; no new telemetry payload or identifiers are exported.

Verification: `go test -count=1 -cover ./internal/workflow/parallel/` PASS (93.2%); `go vet ./internal/workflow/parallel/` PASS; `go test -p 1 -count=1 ./internal/workflow/conformance/bulkack/ ./internal/workflow/conformance/leavereturn/` PASS; compiler/frontier/subworkflow suites PASS via `go test -p 1 -count=1 ./internal/workflow/ ./internal/workflow/frontier/ ./internal/workflow/steps/subworkflow/`. `npm run test` PASS, although its root run also discovered duplicate tests under the existing ignored contracts worktree, so the reported count is not a clean current-checkout count. No user interface changed. These fixes do not implement the still-open durable expansion/orchestration features. Shared-index changes belonging to the other execution/cancellation session were left untouched; no commit or staging was performed. Linux race detection and production-load evidence remain unverified.

Promotion regression verification also passed: `go test -count=1 ./test/workflow/ -run '^TestPromotionWorkflow(CompletesEndToEnd|RejectedAtManagerApprovalWritesNoFact|RevalidationBlocksEndsBlocked|SurvivesRestartDuringApprovalAndWait|DuplicateTimerFireAndDuplicateResumeAreIdempotent)$'` (embedded PostgreSQL, 38.5 seconds). Planning-registry validation, code-style and final diff checks passed. Full commit gates were not run against the other session's staged files; explicit package coverage above covers this change.

## Workflow continuation integrity follow-up (WF-STEP-019/020)

Paused the browser designer review at the user's request. Its Equipment Request draft remains unpublished. Switched to backend continuation review without changing the concurrently edited designer.

The SIGNAL resolver deduplicated against rejected entries as well as accepted entries. Five new red regressions proved invalid-signature and wrong-tenant deliveries could suppress later valid work, corrected payloads were wrongly treated as conflicts, and repeated late/out-of-order refusals were relabeled successful duplicates. The lookup now uses accepted evidence only, preserving every refusal record and still rejecting different bytes after a genuine acceptance. This tests the pure resolver and in-memory audit wrapper, not production ingress persistence.

The WAIT resolver checked only whether a requirement digest existed and whether replay named it. Twelve red mutation cases proved modified requirement fields and replay content were accepted; foreign replay binding was already rejected. Both existing canonical digests are now recomputed before use, with typed errors and no returned resolution on mismatch. Unchanged records replay exactly, including without a new clock value. This is integrity validation, not authentication: trusted storage and authorization remain required.

Verification: `go test -count=1 -cover ./internal/workflow/steps/signal/` PASS (87.6%); `go test -count=1 -cover ./internal/workflow/steps/wait/` PASS (88.3%); `go vet` on both packages PASS; code-style gate PASS. `go test -count=1 ./test/workflow/ -run '^TestPromotionWorkflow(CompletesEndToEnd|SurvivesRestartDuringApprovalAndWait|DuplicateTimerFireAndDuplicateResumeAreIdempotent)$'` PASS (embedded PostgreSQL). No UI changes, live-provider checks, race-detector run or production-readiness claim. No commit or staging: the shared index already contains another session's execution/cancellation changes and overlapping planning files; those were left untouched.

## WF-EXT-001 and WF-REV-001 closure

Two 8-lane todos were independently re-verified and ticked. `WF-EXT-001` was a real served-path defect: `promotionStepRunner.RunsInTransaction` claimed only `execute_promotion` while the `ReleaseHold` port refuses to run outside the step transaction, so `compensate_budget_hold` could never succeed; the driver now claims it on `PLAN_EXECUTE`, with the contradicted composition assertion updated and a new PRIMARY/Integration/Fault suite. `WF-REV-001` adds the missing cancellation discharge driver: recorded compensation obligations run in reverse commit order and the instance lands in `CANCELLED` or `REPAIR_REQUIRED`, never stranded in `CANCELLING`. Full packages pass (`internal/platform/execution` 80.2%, `internal/workflow/cancellation` 78.0%, both above the 70% floor), `gofmt`/`go vet` clean, and the regenerated todo registry matches markdown at 2228 todos. The two share one commit because `WF-REV-001` depends on `WF-EXT-001`.

Deliberately left open: `WF-CAP-010` has code and passing tests but its dependency `WF-EXT-005` is still unchecked, so ticking it would violate the completion gate; `WF-EXT-005` also blocks `WF-DATA-001` and is the next bottleneck. The lane-attested `REV-101-07`, `REV-102-07`, `REV-103-06`, `REV-021-02` and `REV-040-02` ticks in the tree were not independently re-run here. The registry regeneration also mirrors other sessions' markdown ticks (`PROMO-EXEC-003/007`, `RBAC-RT-005`) and newly added markdown todos (`OBS-025`, `WF-RUN-041/042`, `I18N-004..007`, `REV-013-03/04`), which remain their authors' responsibility.

## Second 8-lane wave: PROMO close-out, two REV closures, two code-complete engines

The second wave was verified claim by claim and five more todos ticked, each green on an independent re-run: `PROMO-EXEC-002` (tunnel chain 50.8s), `PROMO-EXEC-004` (live work queue 77.5s), `PROMO-EXEC-005` (async engine 41.0s plus sweep 24.0s) — the earlier `productui` breakage is gone, `go build ./...` and `go vet` on the package are clean. `REV-003-01` (edge client deleted, `clients` 96.8%, `tools/gen/clients` drift check 71.3%) and `REV-067-01` (page governance, `workspace` 73.4%) also ticked; both have no dependencies. All dependency checks were done against the live backlog before ticking.

Code-complete but correctly unticked: `WF-EXT-005` (new `internal/workflow/execute/capabilityrunner`, 91.6%) cannot tick while `WF-EXT-004` is open; `WF-REV-015` (new `internal/privacy/payloadvault`, 73.6%, dep `WF-REV-014` done) cannot tick until the `FIELD_LEVEL` disposition rows and field migration land; `RBAC-RT-006` server gates pass but its GREEN needs the UI fallback removal, blocked on another session's dirty `productui/model.go`. `WF-CAP`/`WF-TEST` (29) and `WF-DATA` (42) remain RED-held behind `WF-EXT-004/005/012/022` and `WF-REV-015`. Commits are still held: `driftgate` stays red on six uncommitted packages, four of them foreign (`clockinject`, `errlint`); the fixture cannot be refreshed without claiming other sessions' code.

## Third 8-lane wave: EXT-002 unlocks the chain, REV-002/003/004 close

The third wave pushed the dependency frontier and four more todos ticked, each re-run independently: `WF-EXT-002` (registrations, 51.8s), `WF-REV-002` (compensate executor, 92.0s), `WF-REV-003` (reversibility declaration, fast pure package) and `WF-REV-004` (admission release, 46.0s); `gofmt`/`go vet` clean on all touched packages. Ticking `WF-EXT-002` unblocks `WF-EXT-003` and `WF-EXT-004`, which in turn unblock `WF-EXT-005` → `WF-DATA-001` → `WF-CAP`. `WF-REV-005` is now unblocked too (deps `001`+`003` ticked); `WF-REV-006` still waits on `WF-EXT-011/012`.

Integrity note: one lane claimed `REV-003-02` complete with a new coordinator, but `coordinator.go`/`rev00302_test.go` are absent from disk and no `TestTodo_REV_003_02` exists anywhere — unticked, no evidence to tick. `REV-009-01` files exist but its evidence was truncated in the handoff and not yet re-run. Commits remain held on the same `driftgate` foreign-package block.

## Late third-wave handoff: RBAC-RT-004, REV-009-02, INTAPI-009 close

A delayed reconciliation of the same wave added three closures, each re-run independently before ticking: `RBAC-RT-004` (all three transport packages), `REV-009-02` (primary 43.0s plus cmd/hcmnext Integration/Fault 24.2s) and `INTAPI-009` (pair, fast); `gofmt`/`go vet` clean. Judgment call recorded: `RBAC-RT-004` ticked with supervision unwired because nil fails closed (deny), the outsider cases pass, and `RBAC-RT-007` is follow-up wiring, not a listed dependency. That brings the verified total to 14 todos across the three waves; commits still wait on the `driftgate` foreign-package block.

## NaaS completion audit and verification repairs

NAAS-001..003 have implementation and passing targeted evidence, but none is being silently certified through the remaining composed gates. NAAS-004 and NAAS-005 are genuinely unimplemented: there is no independent signed-cursor recipient API with read/pin/archive controls, and no million-row multi-tenant/concurrent endpoint or migration/recovery qualification. The 100,001-row storage benchmark is not evidence for either. Updated todos and regenerated the registry to make that distinction explicit.

The deleted legacy edge clients prevented the bootstrap harness from compiling. A test-only adapter now preserves its existing request convention over the canonical generated clients, including request headers. The generated client already returns owned errors, so the authority-refusal assertion now inspects that owned envelope rather than trying to decode it a second time as a raw Connect error. NaaS integration/security pass with `go test ./test/bootstrap -run '^TestTodo_NAAS_001' -count=1 -timeout=10m`. The wider journey/execute regression run exposed that one stale assertion; its corrected case passes with `go test ./test/bootstrap -run '^TestExecuteIntentIsRefusedWithoutExecutionAuthority$' -count=1 -timeout=10m` (run in a combined pattern). The other cases in the earlier broader run passed; a fresh complete bootstrap suite is not claimed.

Manual Codex-browser review identified a shared Chromium disclosure problem: a floating panel can paint while its zero-height anonymous details-content box omits text from accessibility. Explicit display:contents and visible content on OPEN disclosures fixes the bell and confirmation surfaces while preserving native closed behavior. Removed the exploratory platform-specific modal-layout change after finding this smaller shared CSS fix. Also separated the action-bar Cancel's text sizing from the header's square close glyph, preserving wrapping for long localized buttons. Desktop, 320px and 390px dark layouts were inspected; Tab cycles inside review, Escape closes it and restores the trigger, and the bell exposes meaningful link names instead of URLs alone. No cancellation was submitted. Full light/RTL visual closure remains outstanding.

The latest full inbox suite passes at 81.5% with new requester-status replay/read-state, archive, recipient/tenant isolation, rollback and invalid-input tests: `go test ./internal/data/inbox -count=1 -cover -timeout=10m`. Product UI passes at 92.3% (`go test ./internal/humanwork/productui -count=1 -cover`); journey render passes at 96.4% and passes again after final sizing changes. Focused NAAS/Popover/PROMOUX_010 tests, todo-registry tests, code-style and vet for inbox/productui/journey-render/bootstrap pass. Known Windows unlink errors occurred after explicit package PASS. Rebuilt the dev binary on 8096 with migrations and scheduler disabled. The transient promotion SIMULATE composition failure cleared as concurrent edits stabilized; those files were not overwritten. No live migration, race/CI certification, final staged gate or commit is claimed. The shared index still contains other work and was not changed.

## Chat backend integrity follow-up (CHAT-022/024/047)

The recipient-state pass enforced current membership, tenant/home identity, monotonic read positions and bounded unread counts; reaction listing now uses current authority and bounded pagination, and pins use current-membership visibility. These are backend slices of the still-open CHAT-022 and CHAT-024 contracts. The parent chat acceptance suite passed, while release-wide and race evidence remain open.

Core chat audit had a commit-order defect: the application wrapper appended the record after the canonical mutation committed and returned the audit error as though the mutation failed. Core conversation, membership, post, reaction and pin event emission now inserts the outbox row, body-free audit event and inventory metadata in the canonical chat transaction. An audit insertion fault rolls the mutation back; idempotent post replay does not duplicate audit evidence. Tenant-level sequence assignment and the durable record adapter's returned event identity were checked under concurrent two-conversation writes. Reconciliation reports outbox entries missing their core audit projection. Migration `00007` makes audit events immutable. The digest uses the canonical records digest and excludes message bodies. The share-link operation only creates a locator; it is not a persisted share mutation. App installation, upgrade and status now use a tenant-scoped revision CAS and the same canonical audit projection in one transaction. Real PostgreSQL fault tests reject installation and status mutations without committing them, then verify retries and stale-write conflict without duplicate outbox/audit rows. Classification, moderation and complete pilot evidence remain open, so CHAT-047 stays unticked.

Verification after the concurrent recipient fixture correction: `go test -p 1 -count=1 -cover ./internal/data/chatstore/ ./internal/data/chatrecordstore/` PASS, 74.2% and 82.3% coverage; `go vet -p 1 ./internal/data/chatstore/ ./internal/data/chatrecordstore/ ./internal/collaboration/chatrecords/` PASS; changed Go files are gofmt clean. Focused real-PostgreSQL tests cover atomic commit and rollback, replay, concurrent audit identity and missing-projection reconciliation. The parent run of `./test/chat` passed; focused application CHAT tests passed after another contributor repaired shared constructor call sites. These are local runs, not a Linux race/CI or full staged-gate certification. No files were staged or committed in this follow-up.

## Grouped commit preparation and integration repairs

The user requested grouped commits of the shared backlog. The pre-existing staged WF-EXT-001/WF-REV-001 batch remains separate; path-limited commits preserve that index and all working-tree edits. No branch, worktree, reset, manual stash, hook bypass or remote push is used. The first attempted UI commit correctly stopped at repository-wide vet: the removed hand-written edge client still had consumers in `test/edge` and `test/serve`. Those harnesses now call the canonical generated clients directly, preserve authorization headers and inspect the already-decoded owned error. The external serve fixture also supplies the newly required independent page-cursor signing key rather than weakening config validation. `go test ./test/edge -count=1 -timeout=10m` and `go test ./test/serve -count=1 -timeout=10m` pass (48.152s and 36.901s). Repository-wide Go vet, API and nine drift checks pass after refreshing the generated library inventory and architecture document. The full commit hook and its staged coverage gate are still required; targeted success does not certify the whole backlog.

Further gate repairs preserve rather than discard work: the untracked duplicate `schema/proto/gen` output was moved to `.artifacts/duplicate-proto-gen-20260921` (34 files identical to canonical `gen`, four older chat files, no application imports). This removes twelve spurious race-policy packages without weakening the policy. The chat storage manifest now spells out each table's seven inherited defaults rather than YAML aliases/merge keys; values and existing route-table overrides are unchanged. Decomposition, race policy, substrate/engine coverage and `go test ./tools/planning/chatdelivery -count=1` pass. The architecture inventory was regenerated after these changes.

The UI commit remains held: one hook run passed staged coverage and latency, but a later retry could not certify `TestInteractionLatencyGate`; a direct diagnostic run exceeded the 100ms 10,000-person query and 75ms large-table p95 budgets under current machine load. Those budgets were not changed. A separate `go test ./internal/workflow/observe -count=1 -cover` failed because the newly added capability runner lacks an observe operation; that runtime group is also not certified. The independent SIGNAL, WAIT and PARALLEL packages pass at 87.6%, 88.3% and 93.2%, respectively, and the late-attribute OTel adapter package passes at 73.7%. Grouping kernel step-integrity fixes together follows the user's grouped-commit request; it does not close the remaining runtime or UI gates.

Landed local commit `6d325785` groups WF-STEP-019/020/021/022: nine files, four regression suites, full pre-commit hook PASS (three-package coverage floor, format, lint, typecheck, vet, race policy, decomposition, drift/API/substrate/engine/library gates, JavaScript suites, nested Go tests and build). Nothing was pushed. The original nine-path staged compensation batch is unchanged. The next path-limited OTel commit attempt stopped at a newly concurrent unformatted `internal/collaboration/chatmedia/media.go`; that active file was not overwritten or reformatted. The UI and OTel files remain uncommitted, along with the larger runtime/chat/editor/planning backlog. This entry records local checks only, not CI certification.

## Full shared-backlog commit preparation

The user explicitly requested all remaining uncommitted code. Staged the 683-file shared backlog together because the runtime, generated contracts, chat, notifications, authorization, UI and policy checks have cross-package dependencies; splitting those changes would leave broken intermediate snapshots. Preserved subsequent concurrent edits for a follow-up rather than overwriting them. Ignored binaries, caches and scratch artifacts are not included. No remote push is requested.

Fixed the previously reported missing capability-runner telemetry boundary with the existing workflow observer, including returned outcome/error and node correlation. Added a failure-path regression that verifies the operation ends exactly once with the node and instance attributes. `go test ./internal/workflow/execute/capabilityrunner -count=1 -cover` PASS (92.9%); `go test ./internal/workflow/observe -count=1 -cover` PASS (96.8%). The full commit hook passed staged formatting, source-style checks and repository-wide Go vet and then entered its changed-package coverage run. That in-progress run is not recorded as a pass or as CI evidence.

The full commit attempt subsequently failed its coverage gate: 18 findings across 130 packages, comprising chat UI coverage at 68.3% against the 70% floor and 17 failing package suites. Failures include missing page-cursor signing keys in integration fixtures, workflow release-count expectations, chat seed room selection, application composition/configuration boundaries, empty-policy login authorization, immutable proposal error expectations, missing execution-host observation boundaries, mutation-in-simulation fixtures, work-queue authorization and error-envelope assertions, a lease-fence regression, generated architecture/OpenAPI drift, planning governance/coverage/traceability findings, and the staticcheck fixture runner. The complete report is in `.artifacts/commit-all-backlog.log`. No full-backlog commit was created, no hook was bypassed, and no changes were discarded or pushed. Later concurrent edits remain separate from the staged snapshot. Repairing these failures or explicitly authorizing an unverified snapshot is needed before this all-code commit can land.

## Grouped commit repair — 2026-09-22

The UI preflight (`go test ./internal/humanwork/productui -tags covergate -count=1 -cover -timeout=10m`) found one stylesheet-contract failure: the new rail menu used `--chat-rail-menu-top` and `--chat-rail-menu-left` without defining them. Added scoped defaults of 80px and 8px, matching the existing fallbacks without changing menu placement behavior. `TestEveryCSSVariableUsedIsDefined` then passed. Latest focused planning checks also passed: todogovernance 95.2%, traceability 97.5%, and `TestGateAndBoundaryCheckersRunAgainstRepository` in 8.074s. The full UI commit hook remains pending at this point.

The collaboration group landed as `5f827c6f`: 120 files covering isolated chat storage, routing, contracts, services, migration/seed tooling and acceptance tests. Its complete local hook passed, including the 23-package coverage gate (21 measured), generated drift, API, policy, JavaScript tests, nested Go tests and build. The application/storage group is next; its snapshot includes the newly added document-store isolation/pool files, whose focused tests passed at 81.0%. The document-hub feature itself remains partial and no backlog checkbox is changed by this commit bookkeeping.

The application/transport commit attempt exposed two additional promotion regressions and a real coverage gap (68.4%, below the unchanged 70% floor). The ordinary-manager fixture mistakenly carried `promotion_operator`, whose explicit diagnostic grant made the no-diagnostics assertion invalid; the fixture now uses only its hiring-manager/manager roles. The History failure was a product defect: a successfully reached blocked END shared the draft-blocked stage and was incorrectly projected as correctable. List and detail now share a durable-record-aware viewer projection; only a succeeded blocked terminal with an instance closes that state. Tests retain open draft/running/skipped-terminal behavior and preserve initiator relationships. `go test ./internal/intent/app -run '^TestBlockedJourneyTerminalProjection$' -count=1` passed, and `go test ./test/workflow -run '^TestPromoUXRealServerPromotionContract$|^TestTodo_UXAUDIT_002$' -count=1 -timeout=10m` passed in 26.557s. Additional application chat boundary tests cover missing dependencies, identity binding, cross-conversation cursor and agent refusal, suspension, governance records, personal-state forwarding and live reference filtering. Full-package coverage is being remeasured before retrying the commit; targeted coverage alone is not presented as the package gate.

The full application coverage diagnostic passed all tests in 1567.806s at 68.3% before the added boundary tests. The new extension/recipient/reference tests pass separately; combining their covered blocks with that diagnostic yields 1,944/2,623 statements (74.11%). This is diagnostic evidence that the unchanged floor is reachable, not a substitute for the next full commit hook. The collaboration retry passed its 23-package coverage gate but found the architecture golden stale after source additions; regenerating it restored all nine drift checks. No threshold was lowered and no check was bypassed.

The user requested that every uncommitted source change be committed in groups. The shared main checkout and staged work are preserved. Commits use explicit path groups and the full hook; no bypass, reset, branch, worktree or push is used. The first two groups landed: `6593139f` (late OTel execution attributes retain the telemetry filtering policy) and `edce6294` (capability authority, narrowed delegation and durable authorization). Both passed their complete local hooks; this is not a CI or Windows race-detector claim.

Repaired the blockers found by the earlier whole-backlog gate: fixture page-cursor keys remain separate from credential keys; SIMULATE fixtures explicitly inherit their suppression overlays; the timer integration fixture now uses separate queue and workflow-instance leases rather than treating queue ownership as instance authority. Fixed bootstrap work-reader setup to persist its role assignment and compare the canonical client's owned permission-denied envelope. Raw workflow inspection now tests an operator, and explicitly verifies the unrelated requester's non-disclosing refusal rather than broadening the inspection policy.

The application now receives chat-media/artifact paths through declared configuration instead of reading environment variables during composition. Migration plan decoding/digest validation moved out of the command into the application boundary, with missing-record, storage-error, cross-workflow, corrupt-plan and digest-mismatch tests. Updated the composition golden and shutdown assertions for the chat service, the release assertion for the shipped hire version, and retired two RBAC known-gap assertions whose denial tests now pass. Removed read-time synthetic Chat grants: configured empty page policy denies login, while default grants remain the durable bootstrap's responsibility. Execution-host notification and downstream operations now open and finish correlated observations.

Targeted local checks passed for the application configuration/composition and live promotion inspector; bootstrap work-queue and P1A authorization; timer takeover/completion; immutable proposal conflict identity; workflow effect-role and simulation fixtures; operator release; and execution observation coverage. The staticcheck clean-fixture test also passes on retry. Windows executable cleanup warnings after a printed package PASS remain the documented platform limitation, not a hidden test failure.

Chat UI regression coverage now exercises delegated control targets, revision-bound deletion, reaction ownership, menu dismissal, absent callbacks, immutable draft copies and LTR/RTL keyboard pane resizing. The package passes at 77.1% locally (previously 62.2% after concurrent UI expansion). No new browser inspection is claimed by this commit-repair pass; previous visual evidence remains in the original entries.

Planning repair names the already-existing signal/column tests explicitly in evidence, normalizes matrix labels to the existing taxonomy and adds missing risk-derived test obligations to unchecked backlog items. Missing notification/hire intent contexts are now explicit. Scheduling-agent dependents and activated-catalog work move no earlier than their existing Phase 2 dependencies; verified provider receipts follow their Phase 3 prerequisite, and requester/editor notifications follow Gate C authoring. No dependency was removed, no new waiver added and no unfinished TODO checked off. Todo governance, traceability and the repository gate/boundary checker test pass after regenerating the registry and capability coverage matrix; remaining grouped commit hooks are still being verified.

The collaboration preflight passes with `go test ./internal/collaboration/... ./internal/transport/chat/... ./internal/transport/chatmedia/... -count=1 -cover -timeout=10m`: every package with executable statements exceeds 70% coverage. The command prints the documented Windows executable-cleanup warning after all package results pass. This preflight does not replace the later commit hook or certify unfinished chat product requirements.

The full application preflight reached its 25-minute limit while setting up a fresh migrated approval fixture; it is not recorded as a passing package run. It also exposed two remaining inspector fixture mismatches. Terminal raw inspection now uses the administrator, and foreign-tenant token roles without durable grants must return the same permission denial and no record for both existing and absent instance IDs. Production authorization is unchanged. All three `TestTodo_PROMO_EXEC_003_LiveInspect` cases pass together (54.820s), and the served approval/control follow-up passes (169.631s). The commit hook still has to run the complete application package under its existing 30-minute limit.

`7f817f14` landed the coupled workflow execution, durable data-flow, approval notification and migration group. Its complete hook passed, including coverage for all 46 selected Go packages, repository-wide vet, nine drift checks, API and architecture gates, frontend tests, nested Go tests and builds. Existing exact-path coverage exceptions were retained; no new exception or threshold change was introduced. The workflow and migration paths are clean after this commit. Collaboration, application, UI and governance groups remain to be committed.

The collaboration commit's three attempts passed all 23 selected packages (21 measurable packages, all above 70%) but stopped in the later repository-wide checks as concurrent UI edits changed: an unformatted WASM file, a temporary diagnostic file removed during vet, then an unformatted callback struct. No collaboration test failed in those attempts. The current UI packages pass targeted vet after the temporary file was removed. Applied only field-alignment formatting to the callback struct, preserving all newly added controls. Application/transport is being committed next while the UI work settles; the remaining collaboration files stay staged and intact. No hook was bypassed.

A fresh preflight after the concurrent photo/menu additions passes with `go test ./internal/humanwork/chatui ./tools/uxqual/cmd/journeywasm -count=1 -cover -timeout=10m`: chat UI 75.7% and frontend state 82.1%. Both packages printed PASS before the documented Windows executable-cleanup warning. These are native package checks, not a new browser review or a replacement for the UI group's complete commit hook.

## Chat conversation row menus — 2026-09-22

Each channel, direct-message and group row now has a separate vertical three-dot button, so the navigation button is never nested inside another button. The popup renders outside the rail scroll region and keeps the row width stable. Its actions use the clicked conversation ID: View details selects that conversation and opens its member pane, closing the mobile drawer; All messages, Mentions only and Muted read and update that conversation's preference using the current revision. The notification write preserves the existing home-tenant field and updates the targeted row only after success. Escape restores trigger focus; Arrow Up/Down and Home/End move among enabled menu items; Tab, outside click, scroll and resize dismiss it. Outside click leaves the clicked control focused. Labels are supplied for English, German and Arabic.

The browser exposed a popup mount race: the initial zero-delay focus callback could run before the reconciler inserted the menu, leaving outside-click dismissal unattached. A bounded animation-frame retry now waits for the menu before focusing and registering listeners. The mobile browser also showed that View details left the conversation drawer open; that action now closes it. The prior reconciliation test leaked the quiet-refresh marker after its unmounted fallback; its cleanup now restores that test-only global.

## Personal chat groups — 2026-09-22

The recipient-owned sidebar layout now supports creating named groups, moving conversations from each row's menu, collapsing groups, and removing a group while returning its conversations to Channels or Direct messages. These actions change only the personal rail, never conversation membership. The original single `all` section did not match the displayed Channels and Direct messages sections, so its collapse action could not persist; new layouts use the displayed defaults and old layouts migrate when loaded. A legacy layout with a newly discovered conversation is reconciled once, without duplicate rows. Default labels are localized in English, German and Arabic.

Review found a queued-write race and a stale-load race. Serialized writes now read the latest model after taking the writer lock, track the revision of the edit they saved, and suppress delayed server reads that began with a pending local edit. Conversation-list adoption preserves current group placement and collapse state while refreshing conversation values. The expanded row menu scrolls inside its viewport without dismissing itself; external scroll still dismisses it.

Focused native group rendering and late-listing tests pass. The JS/WASM group action parser and legacy-layout integration tests pass under Node, including a saved `all` section with a newly discovered conversation and a custom-only saved layout. Browser review in the isolated candidate passed: creating Sidebar check, moving Sales into it, collapsing, reloading, expanding, and removing the group restored Sales to Channels. Default Channels collapse also persisted. Dark desktop, 390×844 and 320×740 views fit without document horizontal overflow; console warnings and errors were empty. Light mode and the full commit hook remain unverified. No files were staged or committed for this change.

Verification: `go test -count=1 ./internal/humanwork/chatui/` passed; `go vet ./internal/humanwork/chatui/ ./internal/humanwork/productui/` passed; the JS/WASM journey test binary compiled and the exact Node sequence `TestRailNotificationSaveUsesTargetDespiteSelectionChange|TestChatControlsPreserveTimelineDOM|TestTodo_REV_091_03_Browser` passed. The fake-RPC test checked target Get/Update identity, revision, mute flags, home tenant and model row while another conversation remained selected. Browser inspection in dark mode passed at desktop, 390×844 and 320×740: the menu stayed within the viewport without document overflow; General's menu left Q4 selected until View details loaded General; keyboard focus moved from Details to All messages and Escape restored the General trigger; clicking global Search closed the menu and kept Search focused; Evelyn's mobile Details action closed the drawer and loaded the correct two members. Console warnings/errors were empty. Light mode was not inspected. These are local checks, not a full hook or CI certification. No files were staged or committed for this menu task.

## Chat render smoothness — 2026-09-22

The workspace already keyed the timeline container and composer, but its messages, date and unread dividers, and sidebar rows were identified only by data attributes. Older-post insertion could therefore shift reconciliation positions and recreate an existing row. Added distinct sibling keys at those dynamic boundaries. The MutationObserver previously read and wrote scroll geometry for every child-list record in a batch; it now settles each affected list once after collecting the records. The existing document-wide deferred sweep still initializes new fields and list anchors. The renderer still constructs visible message nodes on each local update; no unsafe cache of mutable message or preference maps was added.

Local verification: `go test -count=1 ./internal/humanwork/chatui/ ./tools/uxqual/cmd/journeywasm/` passed. The targeted JS/WASM `TestChatControlsPreserveTimelineDOM` passed with an older-message prepend asserting the original article keeps its NodeID. The targeted JS/WASM `TestChatMutationBatchReadsScrollGeometryOnce` passed: 500 row-like mutation targets settle one list with one scroll-height read. This is an operation count, not an FPS measurement. The later combined rerun was stopped after the separate focused tests passed because other broad Go gates were running concurrently. Root browser QA of the isolated candidate is in progress; no staging or commit was performed for this change.

## Chat image viewer — 2026-09-22

Loaded message images now have a named button that opens the existing authorized image URL in a viewport-sized, contained viewer. The viewer attaches outside the timeline to leave draft and scroll state intact. Escape, the close button, and an exact backdrop click dismiss it; Tab stays on the close control and focus returns to the image button without scrolling. Room, principal, and page changes remove the viewer and its document listener. Native chat UI tests passed with the documented Windows executable-cleanup warning, `go vet ./internal/humanwork/chatui/` passed, and the compiled JS/WASM journey test suite passed under Node, including `TestChatControlsPreserveTimelineDOM`.

Dark-mode browser review passed at 1280×720, 390×844 and 320×740. The image expands within the viewport with its aspect ratio preserved; the close control remains visible and there is no document overflow. Click and Enter open it, Tab and Shift+Tab stay on the close control, clicking the image leaves it open, and Escape, close and the backdrop dismiss it while restoring focus without a scroll jump. SPA navigation back to Home removes the overlay. Browser consoles had no warnings or errors. Light mode was not inspected. No staging or commit was performed for this change.

## Chat channel selection stability — 2026-09-22

The active channel already avoided a redundant read once its stream was live. A second click during its first `ListPosts` read still cancelled the open, cleared the timeline, and started another read. `openChatConversation` now leaves a matching in-flight read alone. Review also found that an older A read could finish after A → B → A and clear the newer A opening latch because completion checked only the room ID. Completion now checks the read generation before clearing that latch or draining the newer A's queued sends; it still drains the older room's own sends when a different room is current.

Focused native state tests passed for repeated selection, stale completion, queued sends, and room switches; `go vet ./tools/uxqual/cmd/journeywasm` passed. The executed JS/WASM `TestChatRepeatedClickKeepsOnePendingReadAndLocalRender` passed under Node: a repeated click kept one read and generation with no route revalidation, another channel opened, and a failed read could be retried. The native test command printed `ok` before Go's documented Windows temporary-executable unlink error. Root browser QA of ordinary switches, a Sales double click, and rapid Sales → Design → Sales double click stayed within the chat and shell; the rapid sequence settled on Sales with 34 posts, the selected-room repeat stayed stable, and console diagnostics were empty. The full commit hook remains unverified; no files were staged or committed for this change.

## Chat person details — 2026-09-22

Chat names and photos now open a business-directory profile. Its data comes from the governed `ListWorkers` projection; the person and visible manager use chat's existing preferred-name-plus-surname resolver. The profile exposes job title, department, location, company and business unit when disclosed. Phone and email are empty because this projection contains no authorized business-contact fields. A withheld manager relationship or a manager absent from the admitted listing remains hidden. An unknown person is not filled from a raw chat ID.

Message waits for the authorized profile read. It checks a fresh paginated admitted conversation listing and active same-tenant memberships before creating a direct message with a stable pair key and an empty server name. It suppresses duplicate clicks, stale reads and navigation after the viewer changes person, room or session. A successful result closes the profile, opens the room and focuses its composer. Created and reused rooms enter the visible Direct messages section with the peer's directory name; later conversation refreshes retain that label. No chat post was sent as part of verification.

Focused `go test -count=1 ./tools/uxqual/cmd/journeywasm -run '^TestChatPerson|^TestChatDirectMembers'` and `go vet ./tools/uxqual/cmd/journeywasm` passed. The compiled JS/WASM test binary executed under Node with `-test.run=TestChatPerson|TestChatDirectMembers` and passed. The tests assert first-name preference completion, canonical worker identity, omitted private contact and compensation values, manager withholding, delayed projection state, room reuse, stable create payload, duplicate-click and stale-read guards, local section insertion and directory naming after a listing refresh. UI-focused native and JS/WASM checks were reported passing by the UI owner; these checks are local evidence, not a full hook or CI result.

Initial browser QA loaded profile fields and photos, disabled Message for self, created an Anika DM without sending a message, reused the existing Evelyn DM, found no horizontal overflow at 390px and 320px, and found the attachment viewer unaffected. That QA found short profile names, an opaque new-room label and missing rail placement, plus deferred composer focus. A later live run found an additional title overwrite: the sidebar kept Anika Desai, but a conversation stream event with an empty server name replaced the header/composer label with the opaque room ID. The event adapter now preserves the locally authorized peer name, and delayed listing adoption resolves peer/directory facts that arrive during its RPC. Focused native and executed JS/WASM regressions passed for the event overwrite, delayed listing and both cold-load arrival orders; vet passed.

The rebuilt combined preview and live port 8868 passed the final browser retest: Anika Desai and Daniel Nguyen appeared in the DM sidebar after a cold load; selecting Anika kept her name in the header and composer after the stream and after switching Evelyn → Anika. Full profile/manager names included Evelyn Morgan/Amina Rahman and Anika Desai/Sofia Petrov. Opening the profile focused its heading; Message on the already-open Evelyn DM closed the profile and focused the composer. The New group form opened with its input focused and Cancel closed it. Consoles were empty. The live build hash was `D7CC7CE56B2E387CEFBCAAF5ED5AD1E006D1ED23568A132C11AFC27AA9C4D39C`. The full commit hook remains unverified. No staging or commit was performed for this feature.

## Share message to channel — 2026-09-22

Message More actions now offers Share to channel for posts with text. The dialog previews the original author and text, reads a fresh paginated admitted channel list, excludes the source and direct/group conversations, and requires explicit selection of a public or private destination before Share calls the server's `ForwardPost`. The client leaves source attribution unset; the server derives it, checks source disclosure and destination posting rights, and persists one result for retries with the same idempotency key. Forwarded attribution renders with the destination message. Forwarding copies text only; attached media stays in the source conversation, a localized disclosure says so, and media-only posts cannot use Share. The attachment warning and error alert sit outside the scrollable source preview so long messages cannot hide them. The dialog retains the current room and composer draft and includes English, German and Arabic labels, search, Escape/Cancel, focus restoration, pending and error feedback.

The first isolated preview exposed a fast-result render race: the channel RPC returned while the initial dialog commit was still underway, so the model held 13 destinations but the DOM stayed at Loading. A bounded postcommit revision check now retries only local component invalidation until the dialog matches the model. Search now clears a destination hidden by filtering, and is disabled and ignored while a forward is pending so the response cannot lose its destination. On a narrow screen, dismissal focuses the composer when the message's More button is hidden. Temporary diagnostic logs were removed.

Focused native `go test -count=1 -run 'TestTodo_CHAT_030' ./internal/humanwork/chatui ./tools/uxqual/cmd/journeywasm` and executed JS/WASM `go test -count=1 -exec 'node C:\Users\mreca\Desktop\hcm-next\.artifacts\tmp\wasm_exec_node.js' -run 'TestTodo_CHAT_030' ./tools/uxqual/cmd/journeywasm` passed with task-local Go temp/cache directories and `GOMAXPROCS=1` for WASM. The tests cover picker rendering and action dispatch, public/private eligibility, stale listing and auth reset, duplicate clicks, idempotent error retry and success, attribution, draft/room preservation, and the lost-invalidation regression. Backend durable tests covered public/private forwarding, permission denial before writes, attribution and idempotency. Dark-mode preview QA passed at 1280×720, 390×844 and 320×740: repeated opens loaded the eligible channels, filtering cleared a hidden selection and disabled Share, keyboard Tab stayed inside, Escape/Cancel restored desktop More or mobile composer focus, and there was no overflow or console diagnostic. Browser QA did not send a live post. Light mode and the full commit hook remain unverified; no files were staged or committed for this feature.

The final attachment-message preview passed at 1280×720 and 320×740: the text-only warning stayed visible below the independently scrollable source text, the Share and Cancel actions remained reachable, the eligible channel list loaded, and there was no horizontal overflow. The final build was promoted to live port 8868 with WASM SHA-256 `1021192BFDC9F3E04BAAE608A74D938F8BFE2192CB1E94F50570C48E6390FB2B`. A new browser tab reopened More actions → Share, loaded 13 eligible public/private channels, and closed with Cancel; console warnings and errors were empty. No live post was sent. Existing user tabs were left untouched.

## Chat message link previews — 2026-09-22

Copy link now calls `CreateShareLink` for the captured source post and copies a same-origin `/workspace/app/chat#share=<token>` URL. Drafts, loaded and streamed timeline messages, and thread roots/replies recognize that locator. Each viewer resolves it with `ResolveShareLink` using their current principal; the card displays the admitted source channel, author, message text and attachment count, while denied, deleted and malformed targets use a neutral unavailable state. The destination post body contains only the pasted URL. The source text is held in a bounded client cache, never copied into destination durable content or fetched from an arbitrary external URL. Old `#msg` links resolve only against a message already loaded in the selected room. Opening a card goes to the source channel and attempts to focus the post if it is in the loaded window; older posts are not promised as an exact jump. English, German and Arabic copy is present.

The cache admits at most 24 locators, reauthorizes after 30 seconds, hides expired content during a new read, and rejects delayed results after a session or membership reset. Draft previews debounce on locator changes, so ordinary typing after a paste does not cancel the preview. A bounded render invalidation updates loaded and streamed cards; the draft preview has its own capped scroll area, and Jump to newest is anchored in the timeline above the expanding composer. Backend read-only review confirmed same-origin parsing, tenant/conversation response checks, viewer authorization and stale-response guards. An unwatched source-permission revocation can leave a previously authorized card visible until its 30-second cache expiry.

Focused native parser, cache, render and journey-state tests passed, as did executed JS/WASM fake-RPC resolution for initial and live messages, drafts, unavailable/cross-tenant responses and stale authentication. Dark-mode browser QA passed Copy link → paste → authorized draft card, opening the source channel and neutral unavailable state; desktop, 390px and 320px layouts had no horizontal overflow. At 320×740 the document and scroll widths were both 320px, and Jump to newest ended at 395.25px above the composer beginning at 405.25px. A fresh tab opened a copied General message link directly into General, confirming routing from outside the current channel. Drafts were restored after QA and the user's existing deliverable tab was untouched. Final live port 8868 served a 46,501,773-byte WASM with SHA-256 `F39FB7D44AFFFA09684F84964857C62AFA6C765037624D085F23740318BD1BF7`; its author, source channel and body card rendered without console errors. No live post was sent during browser QA. Light mode and the full commit hook remain unverified. No files were staged or committed for this feature.

## Copy chat message contents — 2026-09-22

A fresh live tab on port 8868 showed the Copy message contents menu with a clean console. The temporary QA tab was closed and the existing user review tab was left open.

The message More actions menu now offers Copy message contents for timeline posts, thread parents, and replies. It copies the displayed post's plain body exactly, retaining line breaks, and leaves attachments and metadata out. Textless posts display a disabled action with an explanation. The browser callback reads only a currently loaded message in the selected room and writes during the click's user activation; unknown or closed-thread IDs cannot reach the clipboard. Success, rejection, and missing clipboard support produce localized transient notices. Native `go test -count=1 ./internal/humanwork/chatui`, focused product UI Chat tests, the executed JS/WASM `TestChatCopyContentsClipboardAndThreadLookup`, and narrow vet passed. Preview QA copied exact multiline text from a timeline menu and a thread reply menu and observed the success notice within its four-second lifetime. At 320px, the initially hidden More actions button was unreachable; a first CSS cut moved the button between pointerdown and click. The final cut keeps the kebab fixed at the row's top end while other actions expand to its left on focus. First-click menu opening passed at 320px, with the menu inside 96–302px of the 320px viewport; at 390px it fit within 166–372px. No horizontal overflow or console errors appeared. Live port 8868 returned HTTP 200 and served the promoted 47,359,447-byte WASM with SHA-256 `8005393279BD40AEFC34E321093A1C3617D183CE358B92924A2AFB50A64DC4CB`; the isolated preview was retired. The full shared-index hook and coverage remain unverified; the shared index was untouched.

## Channel todo records — 2026-09-22

Each channel todo mutation now records an inventory entry and an append-only revision containing the prior and resulting checklist content in the same database transaction. Chat backup and restore include the current checklist and its revision history; a PostgreSQL test verified both versions survive restore and another tenant's snapshot excludes them. Existing chat retention settings remain a dry run with no deletion worker. Chat legal holds do not fan out to inventory entries and todo entries carry no hold IDs, so the current hold/disposition path does not yet protect them. A scoped hold contract and enforcement are required before claiming full retention or hold support for channel todos.

## Team and project channel widgets — 2026-09-22

Added channel-scoped informal team roles and project planning milestones to team and project channel widgets. Pin and unpin are restricted to managers. Focused backend, channel-record and recovery, and UI test evidence passed. Browser QA on r17, port 8888, scored 9.1/10.

Partial verification: Rafael's member account could not exercise live pin/unpin. Retention purge remains dry-run only. The full suite still has unrelated failures; no full-suite pass is claimed. No files were staged or committed for this documentation closeout.

## Main chat composer styling refinement — 2026-09-22

The main chat composer now has a three-line writing area, a separate compact toolbar, and an accessible label for unavailable GIF support. r18 on port 8888 passed visual QA at desktop, 390px and 320px, including German and Arabic RTL, with a 9.2/10 score; thread regression passed. Native tests, vet and focused WASM checks passed. The full WASM run remains limited by the preexisting SSR DOM adapter issue. Light appearance was not tested.
